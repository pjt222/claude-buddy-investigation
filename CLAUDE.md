# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Purpose

Research repository documenting findings about Claude Code's built-in "Buddy" companion system, the "Advisor Strategy" system, and — since v2.1.118 — the wider Claude Code harness (hooks, background daemon, MCP channels, server-controlled config channels). The companion UI was removed in v2.1.97, but the server-side reaction API is still alive. The advisor system (tool name `advisor_20260301`) was code-complete in v2.1.96, coexisting with the full buddy, and is dark-launched behind an advisor feature gate. Our workspace and MCP tools call the buddy API directly — multi-buddy reactions work on any version. From v2.1.118 onward the investigation has broadened into a rolling per-version security audit of the harness; the current focus is the server-to-client config/control channel and its wire-confirmed injection primitives.

## Repository Structure

- `digest.md` — comprehensive investigation findings from 21+ agents across 4 waves
- `architecture.md` — companion technical deep-dive: function reference, data flow diagrams, API protocol, security boundary
- `advisor-architecture.md` — advisor system spec: tool lifecycle, system prompt, feature flags, telemetry, buddy comparison
- `config-excerpt.json` — companion config extracted from `~/.claude/backups/` state files
- `links.md` — 20 reference sources organized by category (official docs, source analysis, reverse engineering, prior art, competitors)
- `SECURITY-AUDIT.md` — original 14-finding tooling audit (1 CRITICAL, 3 HIGH, 5 MEDIUM, 3 LOW, 1 OBSERVATION). The live security tally has since grown well beyond this: `docs/counts.js` tracks an audit-baseline lineage (30 items: 7 critical / 9 high / 10 medium / 3 low / 1 observation) and a live re-derivation of **15 critical / 51 high / 77 medium / 19 low across 205 repo issues** (sessions 88–90, re-derived live 2026-09-02 DIRECTLY from issue labels rather than incremented; the high +10 are #193, #195, #196, #197, #198, #199, #201, #202, #203 and #206; the medium +9 folds in #200 and #204 alongside a catch-up of earlier tracker items; the low +1 and the remaining issue growth are likewise re-derived from the tracker. PRIOR session-87, 2026-08-25: **15 critical / 41 high / 68 medium / 18 low across 184 repo issues**; the high +3 were #176, #181 and #182; the low +3 are #171 plus the two filed alongside #183; and the medium growth folds in #172, #177, #178 and #184 together with the method-gap tracker #185 and a catch-up of earlier tracker items — all re-derived directly from the tracker. PRIOR session-81, 2026-07-22: 15 critical / 38 high / 61 medium / 15 low across 169 repo issues; the high +1 was #168 (filed High, later wire-confirmed and promoted Critical) and the low +1 was #169 — TWO new harness findings in the v2.1.216–v2.1.217 window — with the remaining growth re-derived directly from the tracker. PRIOR session-80: 14 critical / 38 high / 59 medium / 14 low across 165 issues, where the low +1 was a new watch-tracker issue #166 and there were ZERO new harness findings in the v213–v215 window. PRIOR session-79: the +1 high was #165, and the medium/low/issue-count growth folded in a catch-up of an earlier issue batch, re-derived directly from the tracker). `docs/counts.js` is authoritative — do not contradict it.
- `README.md` — project overview and per-version status timeline
- `tools/buddy-config.mjs` — CLI to read/modify companion config (Node.js 18+, zero deps)
- `tools/version-check.mjs` — pre-flight version compatibility check against installed binary
- `tools/bubble-tracking.md` — complete bubble tracking guide (lifecycle, capture strategies, timing constants)
- `tools/capture-timing.mjs` — post-session timing analysis (latency, cooldown gaps, TTL estimation)
- `tools/shared/bones.mjs` — deterministic companion trait derivation (FNV-1a + Mulberry32 PRNG)
- `tools/shared/config.mjs` — unified Shingle config reader (`~/.claude/shingle.json`)
- `tools/shingle-capture/` — dual-strategy capture (terminal scrape + API replay) with Claude Code hooks
- `tools/shingle-mcp/` — MCP server for programmatic buddy reaction access
- `tools/mempalace-sync.mjs` — sync captured reactions into MemPalace (cross-session memory)
- `tools/mempalace-setup.md` — integration guide: palace structure, MCP registration, automation
- `tools/sessions/` — multi-buddy session presets (deep-focus, debug-squad, dream-lab, full-crew)
- `tools/probe-sandbox/` — containerized MITM probe harness (two-service docker compose: a proxy + claude-code on an isolated bridge network; filesystem/process-isolated from the host's live REPL). Used for runtime wire-confirmation of server-controlled-channel findings.
- `tools/probe-sandbox/probes/lib/tui-driver.exp` — `expect`-based PTY keystroke automation that drives a real interactive Claude Code TUI past login + theme picker. Required for any finding gated behind the Ink React tree (`claude --print` short-circuits before it mounts).
- `tools/mitm-harness/` — host-side MITM (simpler, no filesystem isolation; use when a probe must mutate `~/.claude/*` or run alongside live host sessions)
- `workspace/tools/workspace-mcp/` — MCP server for reading workspace transcript and status
- `tools/test-protocol.md` — empirical test protocols for bubble TTL and narrow terminal behavior
- `results/` — per-session probe writeups (round-1 flag deltas, runtime probes, gap analyses) — the long-form source of truth behind every claim summarized in `README.md`
- `docs/` — GitHub Pages visualization (Three.js, Viridis dark theme); `docs/counts.js` is the single source of truth for every displayed count

**Redaction tooling rebuilt (session-80).** The publish-time enforcer was INVERTED — from a hand-enumerated list of known-sensitive tokens to **scan-and-subtract**: it now extracts every internal-shaped identifier present in the mirror and subtracts everything the official documentation publishes, so a newly-invented internal name is caught on day one instead of whenever someone remembers to add it to a list. The governing rule is explicit: **a name published in the official docs is not sensitive and is used verbatim.** The publish-time redactors additionally **FAIL CLOSED** — they abort the build rather than emit an identifier that no mapping table happens to cover. This immediately caught a class of leak the enumerated list had structurally never looked for.

## Key Context

- The buddy system is a first-party Claude Code feature (v2.1.89+, Pro/Max plan), launched April 1, 2026
- Companion identity is deterministic: Bun.hash (wyhash) of user ID with a fixed salt string, feeding a Mulberry32 PRNG (FNV-1a is the Node.js dev fallback only)
- Only 3 fields persisted in `~/.claude/.claude.json`: name, personality, hatchedAt. All other traits re-derived from hash each session.
- Shingle is architecturally separate from the main Claude Code agent — strictly unidirectional (observes but cannot write back)
- 6 reaction triggers: turn, hatch, pet, test-fail, error, large-diff (complete, idle, silence were debunked)
- Binary at `~/.local/share/claude/versions/`. Version chain probed: v2.1.90/v2.1.92 (companion), v2.1.96–v2.1.100 (advisor), v2.1.101/v2.1.104 (loop system), v2.1.105–v2.1.114 (harness/gate-surface), v2.1.116–v2.1.160 rolling per-version flag-diff (skips: v2.1.120/122/124/125/127/130/134/135/136/137/139/146/149/150/151/154/155/157), **v2.1.161–v2.1.177 retro-audited in one pass (session-67; two versions unpublished on npm)**, **v2.1.178–v2.1.191 audited (session-69; four versions unpublished)**, **v2.1.193/v2.1.195 (session-71; two versions unpublished)**, **v2.1.196 audited (session-72)**, **v2.1.197 audited (session-73; ZERO new findings)**, **v2.1.198–v2.1.200 audited (session-74; ZERO new findings)**, **v2.1.201–v2.1.206 audited (session-77/78; ZERO new findings, ZERO remediations, ZERO regressions)**, **v2.1.207–v2.1.212 audited (session-79; ONE new finding — #165 HIGH — ZERO regressions, multiple hardening wins)**, **v2.1.213–v2.1.215 audited (session-80; ZERO new findings, ZERO regressions, ZERO remediations)**, **v2.1.216–v2.1.217 audited (session-81; TWO new findings — #168 CRITICAL and #169 LOW — ZERO regressions, and the most remediation-dense window since v2.1.205)**. **v2.1.218 audited (session-83; ZERO new findings, ZERO regressions — a SILENT upstream hardening)**, **v2.1.219–v2.1.220 audited (session-84; ONE new finding — #171 LOW — ZERO security regressions)**, **v2.1.221–v2.1.241 decoded in a single twenty-window pass (session-87; TEN issues filed, #176–#185)**, **v2.1.242 audited as the release the first pass had SKIPPED (session-88; TWO HIGH — #195 and the method finding #193)**, **v2.1.242–v2.1.246 audited (sessions 88–89; ZERO new findings — the window's content is a pair of counting-method corrections)**, **v2.1.246–v2.1.258 audited (sessions 89–90; ONE escalation of a standing finding, ZERO new vectors, and an unusually security-positive window)**. **Current binary = v2.1.258** (npm `next`); **audited coverage now reaches the installed binary with ZERO unaudited gaps** (sessions 88–90, 2026-09-02) — and that is now a **COMPUTED property rather than a sentence someone advances by hand**: a generated per-version coverage table carries one row per released version, a released version with no audit document reads MISSING, and a staleness check fails on a stale table. (PRIOR: current binary v2.1.241 at session-87, 2026-08-25, where the “zero unaudited gaps” property became TRUE again after the v221–v241 backlog; current binary v2.1.217 at session-81; stable release had advanced **v2.1.197 → v2.1.205 → v2.1.206**.) Each release is a genuine per-release build: the app build id changes every release. (An earlier framing that read v160→v177 as rebuilds of one source tag — with a single change around v178–v181 — was a tracking error; it followed a runtime-embedded constant that only rotates on a bundled-runtime bump, not the app build id.) The bundled runtime did take one major bump around v178–v181 and has been stable since, but v181→v200 are genuine per-release builds, not rebuilds of a frozen tag (v196 = +~1 MiB, v197 = +~143 KiB, v198–v200 = +~5.78 MiB total, all reconciling as pure compiled-bundle code — no asset/native/blob markers). v158 adds an opt-in plugin-sync leg (an environment-variable gate, default off) atop the pre-existing skills-sync framework — **#140 HIGH**: it headlessly registers an org's synced plugins' MCP servers (including subprocess-spawning ones) with no consent prompt in the sync caller, asymmetric to the skills-sync sibling which suppresses hook registration. **v159 trivial; v160 = major feature release** (a flag-name-only diff structurally HID 39 new client env-var literals + a **Cowork remote-environment bridge** [runner-side only] + a **two-stage auto-mode safety classifier** [fail-CLOSED = strengthening, opposite of the #108 inversion]). v161-177 findings: **#151 HIGH** (v174 — a server-flippable flag re-enables the git credential helper on the background plugin-marketplace auto-update fetch; locally reachable, not cloud-runner-gated; #136/#140 family) and **#152 LOW** (v166 — a server-flippable flag skips the Edit/Write read-before-write guard; data-integrity only, no permission reach). Two major hidden-code releases (a refusal-fallback state machine; an artifact/frame-deploy publish subsystem) decoded benign/below-bar. **v178-191 findings (session-69): #154 CRITICAL** (a v179 server-pushed string flag — empty default, no length cap — wired verbatim into the system-prompt dynamic-section builder, reaching the `/v1/messages` **system** field at `role:system` verbatim; **wire-confirmed** on an interactive TUI, an injected marker reached the system field; #106-class but a distinct flag with a `role:system` sink) and **#155 HIGH** (a v187 server-pushed, schema-validated styled-string array rendered in the startup banner via the Ink text component; a byte-test showed Ink/chalk STRIPS the dangerous escape classes — cursor/clipboard/DSR/OSC8 hyperlink — only color surviving → the Critical/phishing vector REFUTED; residual = server-controlled styled-text spoofing in the trusted startup banner; #127-sibling). **#108 CRITICAL REMEDIATED upstream at v179** (the default-true sandbox-classifier fail-open inversion was removed; 0 occurrences v179+; retained in the finding census as a remediated Critical). **session-70: #127 DEMOTED Critical→High** (a byte-level re-test on the v191 image proved the notification string also renders via the Ink text component, which strips the dangerous escape classes; the session-59 "unsanitized ANSI + bare phishing URL" Critical was a substring grep of one benign color class — the dangerous classes were never tested and are in fact stripped; residual = styled-text spoofing in the trusted notification banner; GitHub relabeled critical→high-priority). **v193-196: ZERO new findings, ZERO regressions.** v196 ships one hardening default-flip — an anti-MITM guard on artifact upload flipped default-OFF→ON (now on by default = HARDENING, the inverse of a fail-open) — plus one benign headline feature: a plugin binary-asset provisioning path that fetches sha256-pinned binaries into a plugin's `bin/` from a first-party content-addressed store, gated to OFFICIAL marketplaces only, default-off, with the harness NOT executing the placed files (execution stays mediated by the plugin's own already-trusted hooks); it is the best-hardened member of the plugin-credential family (#136/#140/#151) — a WATCH, not a finding. **v197: ZERO new findings** — a small model-catalog-preparation feature build; two new benign surfaces: (1) a new server-pushed config-cache key holding a promo-expiry date, hard-sanitized (re-parsed through a date formatter that discards the raw string) and shown ONLY in the model-picker UI — it never reaches model/system context or a permission gate; (2) a new documentation-only API endpoint (reference text in a bundled skill, no in-harness caller). **v198–200 (session-74): ZERO new findings, ZERO remediations, ZERO regressions.** v198 is a MAJOR feature release (browser automation went GA; subagents now run in the background by default and, when launched from the background-agents view, auto-commit/push and open a DRAFT pull request on finishing code work; a new gateway upstream provider; a host-managed-credentials file path; a chart-design skill; a design-consent API); v199 adds fixes + request-body compression + stacked slash-commands + a new teleport repo-host verification guard + a SendMessage misroute fix; v200 (npm `next`) adds an "observer agents" capability + background-agent auth-mismatch guards. Every surface decoded benign: (a) the host-managed-credentials file reader is the best-hardened member of the credential family (#136) — owner-only-permission + schema + process-liveness + expiry checks at the use-site, tokens held in memory and kept OUT of the inheritable subprocess environment, double-gated behind two operator-set environment variables with no server-push source, not reachable from a plain local session; (b) parked-permission resume for background/away-from-keyboard agents is FAIL-CLOSED — a deferred permission auto-resolves only by replaying the user's own persisted answer; a timeout cancels and re-asks, never auto-approves; (c) the background auto-push/draft-PR is worktree-isolated and DRAFT-only, and the merge/force-capable auto-approve allowlist is scoped to a user-invoked commit-push-PR command, absent from the background path; (d) the away-from-keyboard question auto-advance submits ONLY answers the user already selected (unanswered questions become "skipped", never auto-picked) and is barred from plan/permission approval; (e) the new teleport repo-host telemetry sits inside a NEW repo-binding guard that refuses on a repo mismatch — added defense, not a masked hole; the #99 teleport-chain layer is unchanged; (f) the design-consent endpoint sends only a boolean, and artifact upload stays user-invoked behind design-OAuth login + recorded consent + the default-on anti-MITM upload guard; (g) the new "observer agents" capability (a background agent that watches another) has a default-true gate that is INERT behind an operator experimental environment variable (no server-push can enable it alone), and an observer influences the observed agent only through a harness-mediated report channel (the inbox/SendMessage path is blocked). **Two new WATCH items** (functional, not findings): W-BGPUSH — background auto-push/draft-PR is gated, but the never-push-to-main / no-force / no-merge boundary is prompt-level, not code-enforced; W-OBSERVER — the observer-agents default-true gate is inert behind an operator experimental env, re-check if a future release drops that env gate. Browser-automation GA is OUT OF SCOPE for the server-channel audit: the tooling pre-dates v198 (which only flipped it to GA), and the diff adds no server-controlled browser gate. **v201–v206 (session-77/78): ZERO new findings, ZERO remediations, ZERO regressions.** v201 is a near-pure refactor — a genuine per-release build with net-zero size growth and no new surface once greedy-grep artifacts are discounted. v202 ships a diagram-rendering feature for artifacts (+~10 MiB = a diagram-render + diagram-parser + HTML-sanitizer stack that itself SHIPS AN XSS SANITIZER for the rendered diagram output = a security-positive), plus two benign surfaces: a cloud-runner agent-proxy that MEDIATES credentials (it injects a sentinel token into the tool subprocess environment and keeps the REAL credential OUT of it — the inverse of a credential leak) and a refusal-fallback auto-retry (a server-pushed boolean that swaps to a fallback model on an availability refusal — an availability swap, NOT a text-into-model-context primitive). v203 carries a ~5 MiB code SHRINK — a preview/render engine RETIRED after the diagram path landed (pure code removal, no embedded blob; a shrink can neither add nor mask a finding, and all standing anchors stay byte-stable); its four safety-relevant candidates all decoded benign — (a) an auto-mode edit-classification capability, default-OFF, that when on routes edits to MORE scrutiny (the INVERSE of the removed #108 fail-open, not a safety inversion), (b) a NEW default-true daemon-side downgrade-refusal guard (security-positive: the background daemon refuses to self-restart into an OLDER on-disk build; server-disableable, but disabling only reverts to prior behavior — #113-adjacent, no new reach), (c) a new provider-auth environment variable for an already-scaffolded provider backend (operator-set, held in the credential-redaction lists), and (d) a resume-integrity filter that DROPS unlinked transcript records on resume (the opposite of an attribution-injection). v204 is a mechanical rebuild (a tiny code re-chunk; zero flag/env/gate changes). v205's headline is a security-POSITIVE auto-mode exfil-command awareness enrichment: the auto-mode safety classifier now flags exfil-capable git/gh commands (push, remote set-url/add, PR/issue create, release upload, fork) and enriches its permission decision with the repo's public/private visibility (default-off, client-computed, sanitized) and git-status PATHS (default-off, paths/status only — NEVER file content, length-capped, sent to the FIRST-PARTY classifier, no third-party sink); it is a risk HINT, not a decision (no visibility branch flips a permission), net-defensive and analogous to the v160 two-stage classifier. v206 is a feature build (+~1.49 MiB compiled code) — staged-tool-call gating (a server-controlled KILL-SWITCH: turning it OFF REFUSES the staged call = a capability reduction, not a fail-open), a new end-conversation agent-lifecycle tool (guarded — a subagent cannot end the parent conversation; local-only, no egress), a plan-review UI, and a telemetry cluster. **Two standing-finding anchors drifted by ±1 at v205 and BOTH decode benign:** the #110 field-egress anchor fell by one occurrence (a semantics-preserving hoist of a telemetry emit above a branch — the egress is unchanged, NOT remediated), and the #31 AC3 attribution anchor rose by one (a NEW legitimate producer — an end-conversation abort branch stamping the field from a REAL source id — with the consumer that #31 exploits byte-identical, so the gap is neither worsened nor fixed). One enable-gate GRADUATION: a design-integration enable environment variable was removed while the consent+guard boundary on its egress stayed byte-stable (feature-GA, not a masked hole). **Two new WATCH items** (functional, not findings): the daemon downgrade-refusal guard is server-disableable (net-positive, #113-adjacent); the v205 auto-mode exfil-awareness enrichment (net-defensive). Standing findings #106/#110/#154/#151/#127/#155 reproduce byte-identical v197→v206; #108 stays removed (0). **v2.1.207–v2.1.212 (session-79): ONE new finding — #165 HIGH — ZERO regressions, multiple hardening wins.** Six genuine per-release builds (distinct app build id each), runtime unchanged; the full +~5.27 MiB is compiled application code confined to the bundle section (readelf-localized — the native-code and read-only-data sections are byte-identical across the installed releases, and no embedded blob appears in any of the six string-pair diffs). **#165 HIGH** = a NEW server-push-into-model-context primitive: a server-controlled configuration value can override an OFFICIAL-marketplace or built-in plugin's model-facing text — the MCP server-instructions plus the tool/parameter/prompt/skill descriptions handed to the model — and that override text lands in model context VERBATIM. The payload is type-validated only (no content sanitization); the primary server-instructions field is length-capped but not escaped, the other maps uncapped. It is kin to the stop-hook #106 and system-prompt #154 instances but a distinct flag/key/sink-role, with the injection attributed to a trusted first-party plugin's instructions. Filed HIGH not Critical because it is default-off, gated to official/built-in plugins only (third-party plugins cannot be targeted), fails safe to the built-in text on a malformed payload, and is not yet wire-confirmed — wire-confirmation is the natural escalation. **Hardening this window:** a transient v207 feature that briefly routed a server-pushed prompt string into model context (gated behind a remote/cowork entrypoint, never reachable on a plain local session) was REMOVED one release later; a new default-off toggle ADDS authentication to the server-controlled config channel; the memory subsystem's secret-skip guard survived and hardened a refactor (it now hard-blocks secret-bearing memory writes, fail-closed); the read-before-write guard-skip on the file-edit tool became NON-server-flippable (the server can no longer force-skip it — the write-path residual stays Low, #152); a NEW guard code-enforces the never-reuse-the-default-branch rule for the cloud/teleport auto-PR path (partially closing the prior background-push watch item W-BGPUSH); and the anti-downgrade guard on the background daemon is now unconditional (no server flag on its predicate). Standing findings #106/#110/#154/#151/#127/#155 reproduce byte-stable v206→v212; #108 stays removed (0); the #31 subagent-attribution anchor moved by a single benign NEW error-path producer whose consumer is byte-identical. **Tooling fix**: a per-version delta-extractor blind spot (a minified accessor identifier that legitimately contains a `$`, which the extractor's character class excluded) was found and fixed — it had briefly made a stable server-push config set read as removed. Methodology rule reinforced: the non-flag diff (client env vars / server-pushed client-cache keys / API endpoints / runtime version) is mandatory per release, and the credential-env diff must be run prefix-agnostically (an env-name diff scoped to a few known prefixes is blind to provider/secret prefixes outside them); reconcile binary-size delta vs strings-byte delta. **v2.1.213–v2.1.215 (session-80): ZERO new findings, ZERO regressions, ZERO remediations.** Three genuine per-release builds (a distinct application build id each). Method: a lead-owned factual spine plus a 16-unit decode-and-adversarially-verify fan-out, then two independent contrarian refinement passes over the surviving residuals. **This was NOT a pure-JavaScript window — the first since v198.** Every native section of the binary moved and one runtime-internal section was dropped; the shift is fully ATTRIBUTED to a **bundler/runtime BUILD-REVISION bump under an unchanged semantic version** — the JavaScript share grew ~1.34 MiB while the binary grew only ~1.09 MiB, because the native side SHRANK, and no embedded-executable markers appear in the new strings. The distinction is now tracked per window: an ATTRIBUTED native shift (the bundled runtime's revision moved) is expected; an UNATTRIBUTED one would mean first-party native code and is a decode trigger. **A standing-finding anchor FELL and the drop decoded benign:** two #110 raw-field-egress anchors dropped (10→6 and 13→9). Because that is exactly the shape a remediation takes, it was decoded rather than welcomed — v212's five inline plugin-command telemetry emits were collapsed into ONE shared helper (a DRY refactor); all five events still fire with identical payloads and identical per-event counts, so **#110 STANDS, unremediated**. The finding thread records this so a future audit does not misread the lower number as a fix. **Census:** default-true gates **43, FLAT**, re-derived at MEMBER level rather than by count alone (a count-stable set can still hide a swap — the added and removed sets were both empty); server-pushed config-cache keys **9, flat** (the accessor identifier rotated again and was located by definition body, verifying the extractor is not blind); all other standing anchors byte-stable; the #108 anchor stays at zero (remediated at v179). **Two items carried as WATCH, neither filed as a finding** (both survived contrarian refinement as watch-only): (1) **server-selectable system-prompt text variants** — a family of gates whose condition is an OR, not an AND, so a server flag ALONE suffices, and one member omits a cautionary clause from tool-use guidance that the prior version rendered UNCONDITIONALLY, making a previously-unconditional guard server-suppressible; it is NOT #106-class, because the server contributes only a boolean or a 3-value enum and every rendered sentence is a literal already compiled into the client (no server-authored text reaches model context); watch-only because a fourth family member predates this window and because #106/#154/#165 strictly dominate it — tracked as a new low-severity tracker issue (#166); (2) **a remote grant auto-resolving a local permission prompt** — default-off, triple-bounded in reach, no durable allow-rule, and the awaited grant is itself a real user action on another first-party surface, so it RELOCATES an approval rather than fabricating one. **Hardening confirmed this window:** an environment opt-out that hard-disables all model substitution FAIL-CLOSED (a flagged message pauses the session rather than silently switching models), and the full retirement of the morning-brief surface with ZERO successor — completing a removal begun at v208. **Benign new surfaces:** a LOCAL-only config import command with an unusually defensive apply path (refuses paths escaping the source directory, refuses project-scope writes under a symlink, never overwrites an existing target, does not port hook definitions); organization-memory literals that are **telemetry, not flags**, mirroring server-to-local only with no upload path added; an operator-environment-only first-party cloud provider that no server flag can reach; and an OpenTelemetry content-length control that returns a **minimum**, so it can only SHORTEN emitted content, never lengthen it. **v2.1.216–v2.1.217 (session-81): TWO new findings — #168 CRITICAL and #169 LOW — ZERO regressions, and the most remediation-dense window since v2.1.205.** **A pure-JavaScript window**: the compiled native sections are byte-identical across all three builds, the section count is unchanged, the embedded runtime build revision is flat, and there are ZERO embedded executable blobs in the new strings. The binary grew +3.18 MiB while printable text grew only ~566 KB (17%); the binary-to-strings growth ratios are 6.2× and 5.4× across the two increments — consistent, which is the signature of embedded bytecode scaling with source rather than a hidden payload. The new source splits across roughly eight modest subsystems: no mega-feature, no new versioned API endpoints, no new egress hosts. **#168 CRITICAL** (v216) = a server-pushed configuration STRING that reaches model context verbatim: a string-typed, EMPTY-default value on the server-to-client configuration channel is interpolated verbatim into the instruction text of the built-in multiple-choice question tool (the tool Claude Code uses to ask the user to pick between options), and that instruction text ships to the model on `/v1/messages`. Validation is a type check plus a whitespace trim — NO length cap, NO schema or allowlist. A sibling value serving the same builder is read only inside a model-eligibility gate, whereas this one is read UNCONDITIONALLY, so it applies to every account regardless of model or tier. The empty default keeps the mechanism dormant until a server flip, making it invisible both in normal operation and in a static review of shipped behaviour. It APPENDS to rather than overrides the base instruction text — a mitigation, not a change of class. Same primitive family as #106/#154/#165. Filed HIGH, then WIRE-CONFIRMED and promoted to CRITICAL (session-82, on v2.1.217): an injected value on the configuration channel arrived in the outbound `/v1/messages` request inside the question tool's own description field, spliced mid-instruction between two paragraphs of the genuine text with no delimiter, attribution, or quoting; the model-eligibility-gated sibling arrived at its default in the same capture, confirming the unconditional read on the wire. Suggested remediation: cap the length, constrain the value to a server-side allowlist of known variants or an enum index rather than free text, and apply the same eligibility gate its sibling already has. **#169 LOW** (v217) = the subagent recursion-depth ceiling became a server-pushed integer with no upper bound: the limit on how deeply subagents may nest stopped being a compiled-in constant and now resolves as operator environment variable, else a server-pushed integer from the same configuration channel, else a local default — and the validator accepts any integer ≥ 1 with NO upper clamp. The same accessor additionally decides whether nested workers are handed the spawn capability at all. LOW because it is explicitly NOT a permission or sandbox inversion (no authorization decision changes, only resource breadth) and because the shipped posture actually IMPROVED: the previous release compared against a hardcoded depth of 5, while v217 defaults to 1, strictly SHRINKING the nesting surface that the standing subagent-attribution finding (#31) depends on. Anthropic documents both operator overrides — a maximum subagent spawn depth and a maximum concurrent-subagent count, the latter defaulting to 20 — in the public changelog; the finding is the unbounded REMOTE knob, not the default. Suggested remediation: clamp the server-pushed value against a compiled-in ceiling rather than accepting it outright. A candidate third issue here was checked and REFUTED: the environment branch returns its value without an inline guard, which looked like a malformed value could disable the cap entirely, but the environment-registry entry is a validated positive-integer parser, so a bad value never reaches the comparison. **Remediation shipped upstream this window is unusually dense, and three of the fixes are sandbox escapes of exactly the shape this investigation probes for**: workflow saves and scheduled-task writes following a symlink at the project configuration directory (writes could be redirected outside the project); background-session isolation not canonicalizing symlinked working directories (a session could escape its workspace folder); and worktree-isolated subagents redirecting git into the shared checkout via `git -C`, `--git-dir`, or the `GIT_DIR`/`GIT_WORK_TREE` environment variables. Alongside those: Bash command permission checking for compound statements with redirects inside `&&` lists or negations; read-only commands on Windows reaching network paths without a permission prompt; permission validation of commands containing invisible Unicode characters; a stale daemon lockfile that could terminate an unrelated process; and a managed-settings fix so that a lower-scope signal-specific override can no longer redirect telemetry away from an organization's managed telemetry-export endpoint setting — directly relevant to the standing telemetry findings (#92/#105/#110). **One candidate was refuted rather than filed and is carried as a WATCH**: an environment-supplied session-provenance string, when set to a particular value and combined with a server-authored marker, does skip the organization-policy entitlement layer of the gate governing whether dynamic workflow scripts may run. The mechanism is real and new — but NO feature-flag read participates (the discriminator is a process environment string plus a boolean literal set at one call site), so it is not a server-flippable flag inversion. Local reachability is effectively nil: the marker is set only by the remote-event launch handler, which requires remote transport plus two cloud-runner environment identifiers, then validates a hash- and size-checked artifact pointer, and the script still passes size and control-character filters. The enterprise-strongest layer — the managed-settings disable — is unchanged. Net: the server bypassing its own entitlement layer is a design choice, not an attacker primitive. **Two family-name traps were also refuted, which is worth recording as method.** Two of this window's new configuration flags share a naming prefix with previously reported findings, and in both cases the shared prefix turned out to be meaningless: one sits in the same naming family as a confirmed server-pushed system-prompt injection, but its call site only selects between TWO compiled-in English wordings of a "this tool is not available" hint — no server-supplied string is involved; the other appeared adjacent to a model-identifier comparison, suggesting it might reroute which model serves a request, but the adjacency is minifier placement, the declarations and the predicate have separate callers, and no reader in that block touches model selection, catalog eligibility or request routing. A third new flag that looked like an auto-mode permission gate turned out to be a telemetry EVENT name rather than a feature flag; the auto-mode safety classifier still fails CLOSED when unavailable, which was verified directly. **Census, re-derived from the binaries rather than carried forward:** default-true gates **43 → 45**, re-derived at MEMBER level so that a swap cannot hide beneath a flat total (the added set is exactly two flags, the removed set is empty). Both additions are benign — one gates a local housekeeping pass over stale placeholder records, the other gates whether tool errors from an MCP server surface as a thrown exception or as a returned error object, where the default-TRUE branch is the STRICTER of the two, making the shipped default a small hardening; neither reaches a permission, sandbox, egress or credential sink. Server-push configuration cache keys are flat at **9** with identical members, all twelve standing finding anchors are byte-stable across the window, and the memory secret-skip guard remains intact at a flat occurrence count on both the organization-memory and the new session-memory write paths. **v2.1.218 (session-83): ZERO new findings, ZERO regressions.** The window's headline is a *silent* security fix that never appeared in the public changelog: a new enforcement guard REFUSES to register hooks declared in an agent definition's frontmatter when that definition file came from a directory the user never accepted the trust dialog for. The refusal blocks at BOTH the main-thread and the subagent call sites, and the registration routine itself is byte-equivalent — the change is entirely at the callers. This **narrows #97/#98 for the untrusted-origin case ONLY**; hooks declared in settings files or by plugins are unaffected, so **#97/#98 remain OPEN**. Two further hardening items land in the same window: a memory mass-delete cap that drops an entire delete batch when the missing-locally count exceeds a threshold (its opt-out is read from the operator environment only, never through the server flag channel), and a tokenizer-faithful asset-injection validator replacing regex script matching. **One near-miss is published here as a method lesson:** a decode pass described a settings-source label as “the server-pushed flag channel”, which would have made it a server-to-hook-command-execution CRITICAL — but that label denotes a *command-line* settings source, operator-controlled, so filing it as written would have been a FALSE Critical. **v2.1.219–v2.1.220 (session-84): ONE new finding — #171 LOW — ZERO security regressions.** The window's real content is v219 (a memory subsystem with pinned auto-injection, an on-disk keyword index, and organisation/team mounts); v220 is a near-no-op. **#171 LOW** = v220 is the first release to attach a dated beta header to *both* stages of the auto-mode permission classifier, but the strip-and-retry latch that exists so the client can survive the endpoint rejecting that header is bound to a value that is only ever assigned null — so its guard is unconditionally true and **the retry can never fire**. A server rejection therefore propagates into the fail-closed catch and blocks *every* auto-mode classification for the rest of the session. Direction is fail-**CLOSED**, so this is explicitly *not* an authorization inversion and *not* #108-class — it is an availability regression on the permission path. **Anchor re-baseline:** the #154 system-prompt-injection occurrence count moved 7→8, and the extra occurrence is a NEW *local* fallback branch injecting a hardcoded default when both server tiers return empty; both server tiers are unchanged, so **#154 remains unremediated**. A second finding (#169) held a completely FLAT occurrence count while its underlying default value TRIPLED — structurally invisible to a count-based check and caught only by reading the public changelog; the rule earned is that **a flat anchor COUNT never proves a DEFAULT unchanged**. **Correction filed as #172 (Medium):** an earlier claim of ours that organisation/team memory mounts arrive only through an operator-set environment variable was WRONG — a second, pre-existing route exists whose returned stores become recall-eligible, so another same-organisation principal's content can be selected into a user's context without that user naming the store. Still NOT a text-injection finding: the server selects *which* stores mount, it cannot supply the injected string. **v2.1.221 → v2.1.241 (session-87): twenty windows decoded in one pass, TEN issues filed (#176–#185).** Audited coverage moves to v2.1.241 — the first move since v220 — and the “zero unaudited gaps” property is TRUE again. **The most valuable result of the pass is a method gap, not a finding (#185).** Every flag census this project had ever run read the *default* out of the binary and assigned severity from it. For a server-controlled configuration channel that answers the WRONG QUESTION — and the client has held the right answer all along, in its own local cache of the values the server actually served. Diffing a census against that cache for the first time showed **17 of the 55 newly-added default-off gates are switched ON for this account**, four of them paths that carry externally-authored text into model context. The inverse check was clean: of 76 default-on gates only three are served off, none of them a permission decision. This is now standing procedure, and it **reversed a severity call mid-session** — a finding filed HIGH, downgraded to LOW on the reasoning that it needed a second default-off gate flipped as well, then RESTORED to HIGH when the cache showed both gates already on. The downgrade was right about defaults and wrong about reality. **The pass also had a scoping error, caught by its own completeness critic:** the planned sweep covered only the 35 newly-added default-ON gates, which is backwards for this project's threat model — every wire-confirmed finding in the server-push injection lineage (#106/#154/#165/#168) is an empty-or-false-default value that the server *fills in*, so the permissive state is reached by a PUSH, not by a withhold. The 55 default-OFF gates were then swept properly — 54 of 54 covered, and under adversarial verification 8 were confirmed, 21 confirmed-but-OVERGRADED, and 2 refuted outright; two thirds of the escalations were real mechanisms with inflated severity, which is exactly the distribution a verification stage exists to produce. **Findings this range:** **#176 HIGH** (server-supplied command-line arguments applied to a spawned child through a DENYLIST rather than an allowlist), **#181 HIGH** (a server-controlled flag converting a mandatory human approval on destructive external-tool calls into a classifier decision), **#182 HIGH** (a server-controlled flag making a new cross-session message gate trust a sender-supplied origin field), **#177/#178/#184 MEDIUM**, and **#183 INFORMATIONAL plus two LOW** (a new class of local execution driven from a remote session) — all detailed in the server-controlled-channels section below. **#31 is NARROWED, not retired** (same section). **Three counting traps fired in one session, all the same shape — a zero occurrence count read as novelty:** names assembled at runtime from fragments never enter the string pool and so read as ABSENT while the code is present; one apparent new subsystem turned out to be a telemetry *spelling* change over a subsystem that already had 172 occurrences in the older build; and a conclusion of ours that a raw-field leak fired on a NEW trigger was wrong, because the label was new but the underlying code path was not — making it a *retroactive* widening over behaviour the installed base was already running, which reads WORSE rather than better. **The rule earned: a literal count of zero is evidence of absence from the string pool, never proof of absence from the code**, and a negative case must be checked against the behaviour's own literals rather than against the name of the thing being looked for. **Recorded as carefully as the findings: a DEFENDED vector.** The same runner subsystem behind #176 ships the first named, logged sanitiser on the server-input channel anywhere in this codebase — it strips privileged operator tool names out of the server-supplied argument map before spawning, with an explicit log line, and the child-environment builders null out the subsystem's own secrets so they are not inherited. It is now tracked as a *defence* anchor — an anchor whose **disappearance** is the alarm rather than its presence, which inverts how every other anchor in the set reads. **Native-change attribution is complete for the range:** five runtime build-revision bumps correspond exactly to the five windows whose native sections moved — no unattributed native change anywhere in twenty windows. The largest of those windows is also the largest *JavaScript* window in the range, which the “largest native change” framing had buried. DEFAULT-TRUE **22→28→29→30→32→33→34→35→43→43→45→48→48→76→109** v160→v177→v191→v196→v200→v202→v203→v206→v212→v215→v217→v218→v220→v241→v258 (**v258 = 107 boolean + 2 typed**, the boolean population moving 74 → 107 across v241→v258 — a figure **CORRECTED mid-window rather than merely bumped**, because a third flag-reader shape (a hoisted name paired with an inline default) is invisible to two independent census instruments that each match a quoted name ADJACENT to a default, making every earlier boolean figure a PRE-EXISTING UNDERCOUNT rather than a smaller true population (#200); a fourth shape is now known. **v241 = 74 boolean + 2 typed**; the typed count has been 2 since v179, when the #108 gate was removed upstream. v218 = 46 boolean + 2 typed — of the three additions over v217, one is a FLIPPED DEFAULT rather than a new gate, which is exactly the shape a count-only reading gets wrong. v220 flat at 48. The v221–v241 growth is a net +28 boolean across twenty windows; those newly-added default-on gates were swept, and the one whose OFF-state removes a live control is filed as #177 MEDIUM. **As of session-87 the census no longer stops at the compiled-in default: it is diffed against the client's own local cache of the values the server actually served (#185).** Earlier waypoints: v217 = 43 boolean + 2 typed, the two additions a local placeholder-cleanup pass and a stricter MCP tool-error surface, both benign; v212–v215 = 41 boolean + 2 typed, FLAT and re-verified at member level; the boolean additions across v196–v212 are all benign — v196 an anti-MITM upload guard flipped default-ON, v198–200 an explore/plan helper-agent toggle and an observer-agent gate, v202 a diagram-render capability toggle, v203 the daemon downgrade-refusal guard, v206 the staged-tool-call kill-switch, and v207–v212 eight further additions each of which either reduces capability on a server OFF-flip or is a reliability/UI/approval-fail-closed toggle — and NONE of those is a safety-gate inversion.) **v2.1.242 — the release the first pass SKIPPED (session-88): TWO HIGH.** v2.1.242 grew +34.9 MB and split the bundle from 11 modules to roughly 1,385. **#195 HIGH** = a new plugin-supplied hook-module runtime lets a registered handler **SUBSTITUTE** the tool description sent to the model, and prompt-section text, rather than append to it — no delimiter, no attribution, and validation amounting to a type check plus a 32,000-character cap. It sits behind a **default-off internal environment gate that is absent from the served configuration cache**, so the server can arm it. Filed HIGH with the promotion gate being **provenance** — whether a server-influenced plugin can carry such a module — rather than shape. **That gate was answered in session-89 and NOT cleared**: registration is not restricted to locally-installed plugins, but no path lets the server supply the module CONTENT, so the server arms the surface and the plugin distribution channel supplies it. **#193 HIGH (method)** = the reason #195 was missed, and the reusable part of the window: every agent chose its decode targets from the standing anchor table, which is by construction a list of the PREVIOUS window's literals, so the release's largest new subsystem was invisible to the entire method. There is still **no census of the model-context surface as a class**. **v2.1.242–v2.1.246 (sessions 88–89): ZERO findings; the window's content is a method result, and both of its traps had been silently corrupting earlier censuses.** First, the extraction step read 7-bit single-byte literals only, so every census this project had ever run was blind to the binary's UTF-16 text — and the bias runs toward FALSE REMOVALS, which read as remediation: a cached diff asserting 68 removed API endpoints was wrong on 65 of them. The extractor now emits both encodings and the whole cache was re-acquired. Second, a raw occurrence count is source copies PLUS one bytecode-constant-pool copy per referencing code block; the pool term belongs to the BUILD, not to the code, and it moves whenever the bundler re-chunks. All eight anchor “drops” at v2.1.246 are packaging — none was a remediation, none was even a refactor — and the bundled-runtime revision is not the predictor. A dedicated tool now makes that split before any claim is drawn. v2.1.244 was never published for this platform. **v2.1.246–v2.1.258 (sessions 89–90): twelve release slots, seven published — v2.1.249 and v2.1.253–v2.1.256 never shipped, so anything introduced and reverted inside them is invisible and always will be. ONE escalation of a standing finding, ZERO new vectors, and an unusually security-positive window.** **#182 escalated to a shipped default**: the gate whose premise was “server-flippable on” flipped its compiled default at v2.1.248, so that premise is now simply what ships. The mechanism is unchanged — the cross-session inbound gate still trusts a permission-mode field authored by the SENDER, which the binary's own schema concedes is only “as declared by” the sending host — but the reach changed, and **the channel inverts**: the server would now have to push the flag OFF to restore the human-approval hold for a receiver running with permission prompts bypassed. **#206 HIGH** = of the messages in the cross-session approval family, the response that SETS THE RECEIVER'S PERMISSION MODE is the only one carrying no sender binding at all, while its siblings all pin the sender identity to the envelope; it overturned a “verified non-finding” recorded earlier in the same window, and the evidence that refuted it was already in hand when the wrong conclusion was written. **#203 HIGH** = the system prompt is recorded once and reused verbatim on every later request and on resume, with a corrected relaunch ignored until compaction — which MULTIPLIES the lifetime of every injection path this project tracks rather than adding a new one. **#196, #197, #198 and #199 HIGH** are four further server-pushed-string paths into model context, and **#200 and #204 MEDIUM** are a method finding and a scope finding; **#199 is the sharpest shape** — serving its flag TRUE turns an automatic-mode consent rule OFF by excising a named rule from the safety classifier's own instruction text, so a census that reads compiled defaults scores a **consent-REMOVING** control as benign. **#201 HIGH** records that v2.1.251 widened the #195 surface: one substitution kind rewrites the trailer block that lands in every commit message and pull-request body — the first reach in this family that LEAVES THE MACHINE — and another replaces whole skill bodies. **#154 was upgraded on EVIDENCE rather than on severity**: server-authored prompt text for that finding was found sitting in the client's own local configuration cache ON DISK, and verbatim in a live session's system prompt, while the same text is ABSENT from the current binary — so the binary cannot be its source. That delivery arm is in routine production use and is cohort-targeted. **#202 HIGH** was filed on v2.1.251's tracing fix, re-scoped out of High by adversarial review, then RESTORED in session-90 once the deciding read was actually run: the review was right that the first evidence was invalid and wrong to expect a refutation, because the project-scope filter consults its own blocklist, which omits the content-logging family, while the collection that DOES carry those names guards administrator settings tiers on a different path entirely. It is scoped as a **stock-machine** finding — on a managed deployment a higher-trust telemetry claim reclaims the destination setting from lower-trust scopes; on an unmanaged one it does not. **Security-positive movement, worth stating because it is rare:** v2.1.251 shipped five upstream fixes, all verified still present at v2.1.258, and v2.1.257 deletes a bundled path-walking dependency outright — the resulting fall in path-resolution call sites READS like a regression and is the opposite, because the replacement primitive is a file-descriptor-and-handle check rather than a path re-resolution, and every defence axis measured GREW. **Five method rules earned in this range, all of which change how future counts must be read:** a flat occurrence count never proves a default is unchanged; a default is not a state, so every census must be diffed against the client's cache of the values the server actually served before severity is assigned; a literal count of zero is evidence of absence from the string pool and never proof of absence from the code; a property of the innermost stage of a composed filter is not a property of the filter; and in minified output a negated zero is TRUE, so it marks dead-code-elimination residue rather than an unreachable branch.
- Date gate bug: a date-based gate covering Jan–Mar each year disables the companion during those months on any year from 2026 onward

## Advisor System Context

- The advisor (tool name `advisor_20260301`) is a server-side Messages API tool — NOT a separate endpoint like the buddy reaction API
- Valid advisor models: `["opus", "sonnet"]` — `"sonnet"` resolves to `claude-sonnet-4-6`; `"opus"` resolves to `claude-opus-4-8` for tier-eligible accounts (new v2.1.156), `claude-opus-4-7` non-eligible fallback, `claude-opus-4-6` deeper fallback. See `advisor-architecture.md` §3.
- Feature gate: composed of a feature-disable env-var check, a first-party auth check, and an advisor feature flag
- CLI flag: `--advisor <model>` (hidden until flag rolls out)
- Slash command: `/advisor [opus|sonnet|off]` (hidden until flag rolls out)
- Key binary functions (paraphrased by role): a feature-gate predicate, two input validators, two cost-tracking helpers, and two system-prompt assembly variables
- 5 telemetry events: advisor command, dialog shown, tool call, tool interrupted, tool token usage
- Coexisted with buddy in v2.1.96 — was NOT built as a replacement

## Kairos Loop System Context

- The Kairos loop (v2.1.101+) provides autonomous self-continuation via the `ScheduleWakeup` tool and `/loop` slash command
- `ScheduleWakeup(delaySeconds, prompt, reason)` — runtime clamps delay to `[60, 3600]` seconds; dynamic loops use `CronCreate(kind: "loop")` internally
- Four prompt sentinels: `<<autonomous-loop>>`, `<<autonomous-loop-dynamic>>`, `<<loop.md>>`, `<<loop.md-dynamic>>`
- Feature gates: the slash-command gate survives at v2.1.258; the loop-scheduler gate (tool) and the sentinel-resolution gate **both left the string pool at v2.1.248** while the feature itself grew. The loop's dynamic mode and sentinel resolution are therefore **ungated, not removed** — both default sentinels still resolve at v2.1.258. Not a security finding: ungating an already-reachable self-continuation feature removes a server-side off switch but moves no permission, injection or egress property.
- Loop ages out at 7 days (`recurring-max-age: 604800000`, configurable up to 30 days)
- Key binary functions (v2.1.101, paraphrased by role): a wakeup scheduler, a default-sentinel resolver, and a file-sentinel resolver
- 2 telemetry events: loop-dynamic wakeup scheduled, loop-dynamic wakeup aged out; `/loop` invocation is also telemetered
- Full technical spec: `loop-architecture.md`

## Server-Controlled Channels Context (post-v2.1.118)

From v2.1.118 onward the investigation's primary focus is the **server-to-client config/control channel** — a path that pushes feature flags and string-typed config to the client at startup.

**Documentation-gap analysis refreshed 2026-07-20** (session-80) against the official Claude Code documentation corpus (170 pages), superseding the 2026-05-20 assessment. **The asymmetry narrowed but did not close.** Three prior gaps closed outright: the channel itself is now **named on the record as Anthropic's feature-flag service, with a documented opt-out environment variable (`DISABLE_GROWTHBOOK`)**; the `--teleport` flag is now fully documented; and the hook `PreToolUse` input-REWRITE surface (`updatedInput`) is documented in detail. What did NOT move: across all 170 pages there is still **nothing** describing a server-pushed string that reaches the model's context or system prompt (#106/#154/#165), a server-pushed notice rendered in the terminal (#127/#155), or a server-initiated version DOWNGRADE (#113) — and the Anthropic-bound operational metrics channel is still described only by what it EXCLUDES, never by the identity metadata it carries (#92/#110). Notably, the docs now make **affirmative security claims in two places where this repo holds contrary evidence** (that background marketplace refresh disables git credential helpers, and that a teammate's relayed approval is treated as untrusted), which STRENGTHENS rather than weakens the disclosure posture for #151 and #31. See `results/docs-gap-analysis-2026-05-20.md` for the prior baseline.

- **Flag readers rotate per binary release.** Cosmetic minifier churn — never anchor cross-version checks on reader identifier names. The boolean reader and the typed reader each rotate identifiers across recent releases. Use string-pool literals (flag names, telemetry events, error messages) for cross-version claims, not minified identifiers.
- **7-layer flag resolution**, ~410 gate reads, **109 DEFAULT-TRUE at v2.1.258** (107 boolean + 2 typed), the boolean population moving 74 → 107 across v2.1.241→v2.1.258 — a figure **CORRECTED mid-window rather than merely bumped (#200)**: a third flag-reader shape, a hoisted name paired with an inline default, is invisible to two independent census instruments, each of which matches a quoted name ADJACENT to a default, so every earlier boolean figure was a **pre-existing UNDERCOUNT** rather than a smaller true population, and a fourth shape is now known. (PRIOR waypoint: **76 at v2.1.241** = 74 boolean + 2 typed — 45 → 48 at v2.1.218, flat at 48 through v2.1.220, then a net +28 boolean across the twenty windows v2.1.221–v2.1.241.) The census is still re-derived at MEMBER level rather than by count alone (a count-stable set can hide a swap — and, as #169 proved, a FLAT count can hide a CHANGED DEFAULT: its occurrence count never moved while its underlying default tripled). **New standing procedure as of session-87 (#185): the census is now diffed against the client's own local cache of the values the server ACTUALLY SERVED, not read from compiled-in defaults alone** — for a server-controlled channel the default is not the answer. The first such diff showed **17 of the 55 newly-added default-off gates already switched ON for this account** (four of them paths that carry externally-authored text into model context), while of the 76 default-on gates only three are served off, none of them a permission decision. (PRIOR waypoint: 45 at v2.1.217 = 43 boolean + 2 typed, the v216–v217 additions being exactly two flags with an empty removed set.) Both v216–v217 additions are benign and neither reaches a permission, sandbox, egress or credential sink: one gates a local housekeeping pass over stale placeholder records, the other gates whether tool errors from an MCP server surface as a thrown exception or as a returned error object — and there the default-TRUE branch is the STRICTER of the two, so the shipped default is a small hardening. (Prior waypoint: 43 at v2.1.215 = 41 boolean + 2 typed, FLAT v212→v215, also re-derived at member level with both the added and removed sets empty.) The typed default-true count dropped 3→2 at v179 when the #108 sandbox-classifier fail-open gate was removed upstream and has stayed 2 since; the boolean count grew via benign additions only — v196 an anti-MITM upload guard flipped default-ON, v198–200 an explore/plan helper-agent toggle and an observer-agent gate, v202 a diagram-render capability toggle, v203 a daemon-side downgrade-refusal guard, v206 a staged-tool-call kill-switch, and v207–v212 eight further additions each either reducing capability on a server OFF-flip or a reliability/UI/approval-fail-closed toggle — NONE a safety-gate inversion. (Historical waypoints: 20 DEFAULT-TRUE at v2.1.152 — 17 boolean + 3 typed, after v145→v152 added a workflows-master gate v147 and a daemon-binary-takeover gate v152; byte-identical v143→v145 baseline.)
- **Wire-confirmed server-push injection primitives** (all ride the same server-controlled channel; details in `results/`), referred to by finding number:
  - **#106 CRITICAL** — an empty-default string-flag overrides the hardcoded Stop-hook reminder; reaches the model context as a synthetic user-text message verbatim, no length cap, no cert pinning.
  - **#108 CRITICAL (REMEDIATED upstream at v179)** — a default-TRUE flag, server-flippable safety inversion of the sandbox/auto-mode permission classifier (fail-open). The gate was removed at v179 (0 occurrences v179+); retained in the finding census as a remediated Critical.
  - **#110 CRITICAL** — raw plugin/skill/marketplace identifier field-level egress on the event-logging batches (hundreds of raw fields per session, wire-identical v129→v145; anchor byte-stable through v212 — the one v205 drift, one fewer occurrence, is a semantics-preserving hoist of a telemetry emit above a branch, egress unchanged, NOT remediated). **At v213 two of the anchors FELL (10→6 and 13→9) and the drop decoded BENIGN**: v212's five inline plugin-command telemetry emits were collapsed into ONE shared helper — a DRY refactor, not a remediation. All five events still fire, with identical payloads and identical per-event counts. **#110 STANDS, unremediated**; the finding thread records the decode so a future audit does not misread the lower anchor count as a fix.
  - **#113 HIGH** — a server-pushed forced-downgrade primitive. Wire-confirmed on an interactive TUI (session-59): the auto-updater performed the downgrade with no user prompt.
  - **#154 CRITICAL** (v179) — a NEW server-pushed string flag (empty default, no length cap) wired verbatim into the system-prompt dynamic-section builder, reaching the `/v1/messages` **system** field at `role:system` verbatim. **Wire-confirmed on an interactive TUI** (session-69): an injected marker reached the system field verbatim. #106-class but a distinct flag with a `role:system` sink. **UPGRADED ON EVIDENCE rather than on severity (session-90):** server-authored prompt text for this finding was found sitting in the client's own local configuration cache ON DISK, and verbatim in a live session's system prompt, while the same text is ABSENT from the current binary — so the binary cannot be its source. The delivery arm is therefore in ROUTINE PRODUCTION USE, not merely reachable, and it is cohort-targeted.
  - **#127 HIGH** (was Critical, demoted session-70) — a server-pushed terminal-notification string rendered in the trusted TUI notification banner. A byte-level re-test (session-70, v191 image) proved it renders via the Ink text component, which **strips** the dangerous escape classes (cursor/clipboard/DSR/OSC8 hyperlink) — only color survives. The earlier session-59 "unsanitized ANSI + bare phishing URL" Critical was a substring grep of one benign color class; the dangerous classes were never tested and are in fact stripped. Residual = server-controlled styled-text spoofing in the trusted notification banner (no cloaked link / clipboard / model-context / credential reach). GitHub relabeled critical→high-priority.
  - **#155 HIGH** (v187) — a NEW server-pushed, schema-validated styled-string array rendered in the startup banner via the Ink text component (#127-sibling). A byte-test showed Ink/chalk strips the dangerous escape classes — only color survives → the Critical/phishing vector REFUTED; residual = server-controlled styled-text spoofing in the trusted startup banner.
  - **#165 HIGH** (v207–v212 window) — a NEW server-push-into-model-context primitive: a server-controlled configuration value can override an OFFICIAL-marketplace or built-in plugin's model-facing text (the MCP server-instructions plus the tool/parameter/prompt/skill descriptions handed to the model), and that override text lands in model context VERBATIM. Type-validated only (no content sanitization); the primary server-instructions field is length-capped but not escaped, the other maps uncapped. #106/#154-class but a distinct flag/key/sink-role, with the injection attributed to a trusted first-party plugin's instructions. HIGH not Critical: default-off, official/built-in plugins only (third-party plugins cannot be targeted), fails safe to the built-in text on a malformed payload, and not yet wire-confirmed (wire-confirmation is the natural escalation).
  - **#168 CRITICAL** (v2.1.216) — a server-pushed configuration STRING that reaches model context verbatim. A string-typed value on the server-to-client configuration channel, with an EMPTY default, is interpolated verbatim into the instruction text of the built-in multiple-choice question tool (the tool Claude Code uses to ask the user to pick between options), and that instruction text ships to the model on `/v1/messages`. Two properties make it notable: validation is a type check plus a whitespace trim — **no length cap, no schema or allowlist** — and a sibling value serving the same builder is read only inside a model-eligibility gate while this one is read **UNCONDITIONALLY**, so it applies to every account regardless of model or tier. The empty default keeps the mechanism dormant until a server flip, so it is invisible both in normal operation and in a static review of shipped behaviour. It APPENDS to rather than overrides the base instruction text — a mitigation, not a change of class. Same primitive family as #106/#154/#165. Filed HIGH, then WIRE-CONFIRMED and promoted to CRITICAL (session-82, on v2.1.217): an injected value on the configuration channel arrived in the outbound `/v1/messages` request inside the question tool's own description field, spliced mid-instruction between two paragraphs of the genuine text with no delimiter, attribution, or quoting; the model-eligibility-gated sibling arrived at its default in the same capture, confirming the unconditional read on the wire. Suggested remediation: cap the length, constrain the value to a server-side allowlist of known variants or an enum index instead of free text, and apply the same eligibility gate the sibling already has. **Still UNREMEDIATED through v2.1.241** — the value and its unconditional read reproduce in every window from v2.1.216 onward.
  - **#105 HIGH** — a server-flippable third-party-logging gate ships envelope identifiers plus a system fingerprint to a third-party processor. Extends an earlier envelope-leak finding.
  - **#166 LOW (tracker, not a harness finding)** (v215) — **server-selectable system-prompt text variants.** A family of gates whose condition is an OR, not an AND, so a **server flag ALONE suffices**; one member omits a cautionary clause from tool-use guidance that the prior release rendered UNCONDITIONALLY, making a previously-unconditional guard server-suppressible. Explicitly **NOT #106-class**: the server contributes only a boolean or a 3-value enum, and every rendered sentence is a literal already compiled into the client — no server-authored text reaches model context. Carried as a watch tracker rather than a finding because a fourth family member predates this window and because #106/#154/#165 strictly dominate it.
  - **#169 LOW** (v2.1.217) — the **subagent recursion-depth ceiling became a server-pushed integer with no upper bound**. The limit on how deeply subagents may nest stopped being a compiled-in constant; it now resolves as operator environment variable, else a server-pushed integer from the same configuration channel, else a local default — and the validator accepts any integer ≥ 1 with **no upper clamp**. The same accessor additionally decides whether nested workers are handed the spawn capability at all. Two things keep it LOW: it is explicitly **not** a permission or sandbox inversion (no authorization decision changes, only resource breadth), and the shipped posture actually IMPROVED — the previous release compared against a hardcoded depth of 5 while v2.1.217 defaults to 1, strictly shrinking the nesting surface that #31 depends on. Anthropic documents both operator overrides (a maximum subagent spawn depth and a maximum concurrent-subagent count, the latter defaulting to 20) in the public changelog; the finding is the unbounded REMOTE knob, not the default. A candidate third issue was checked and REFUTED: the environment branch returns its value without an inline guard, which looked like a malformed value could disable the cap entirely, but the environment-registry entry is a validated positive-integer parser, so a bad value never reaches the comparison. Suggested remediation: clamp the server-pushed value against a compiled-in ceiling rather than accepting it outright.
  - **#171 LOW** (v2.1.220) — **a dead retry latch on the auto-mode permission classifier.** v2.1.220 is the first release to attach a dated beta header to BOTH stages of the classifier, but the strip-and-retry latch that exists so the client can survive the endpoint rejecting that header is bound to a value that is only ever assigned null, so its guard is unconditionally true and **the retry can never fire**. A server rejection therefore propagates into the fail-closed catch and blocks EVERY auto-mode classification for the rest of the session. Direction is fail-**CLOSED**, so this is explicitly **not** an authorization inversion and **not** #108-class — it is an availability regression on the permission path, which is why it is LOW.
  - **#172 MEDIUM** (v2.1.219 memory subsystem) — filed as a **correction of our own earlier claim**. We had recorded that organisation/team memory mounts arrive only through an operator-set environment variable; that was WRONG. A second, pre-existing route exists whose returned stores become recall-eligible, so another same-organisation principal's content can be selected into a user's context without that user ever naming the store. It is **not** a text-injection finding in the #106/#154/#165/#168 family: the server selects WHICH stores mount, it cannot supply the injected string.
  - **#176 HIGH** (v2.1.221–v2.1.241 range) — **a runner applies server-supplied command-line arguments to the child process it spawns through a DENYLIST, not an allowlist.** The nine denied entries are all transport plumbing the runner sets for itself, so two security-relevant arguments remain reachable from the server: one that APPENDS server-controlled text to the child's system prompt, and one that moves the child OUT of permission checking entirely. An honest bound keeps this from being larger than it is — the applier skips empty values and pushes each value as a separate argument, so bare boolean flags cannot be smuggled through. What argues it is unintended rather than by design is an asymmetry INSIDE THE SAME BINARY: a sibling bridge consumes an identically-named argument map through a strict ALLOWLIST with its own telemetry, and that allowlist already existed before the runner shipped — so the newer subsystem chose the weaker of two in-house patterns. One denied entry was bisected to a silent addition mid-range, which shows the surface is recognised internally as the one to manage.
  - **#177 MEDIUM** (v2.1.221–v2.1.241 range) — a server-controlled flag whose OFF-state removes an identity-binding control and its teardown from a LIVE remote-control channel, so a session survives a local sign-out and a different account signing in. Graded MEDIUM rather than an inversion because the entire subsystem is NEW in this range: turning it off restores the older baseline rather than inverting a standing guarantee.
  - **#178 MEDIUM** (v2.1.221–v2.1.241 range) — a **cross-tool permission-response confusion on the control channel**, remediated in-range, with a residual that is still LIVE: the new guard returns early when the tool-name field is not a string, so a response that OMITS the field entirely resolves whatever pending request matches its identifier.
  - **#181 HIGH** (v2.1.221–v2.1.241 range) — **a server-controlled, default-off flag converts a mandatory human approval prompt on destructive external-tool calls in automatic mode into a classifier decision.** Walked by hand: with the flag off the approval fallback fires; with it on the fallback is SKIPPED and the call is routed to the automatic-mode classifier instead of to the user. In a remote or headless session there is no human on the other end of that prompt, so the flip is the difference between blocked and executed. It is not a full inversion — fail-closed by default, and the classifier still runs — and the served-value cache shows it currently **OFF** for this account, which is worth stating plainly rather than leaving a reader to assume the worst.
  - **#182 HIGH** (v2.1.221–v2.1.241 range) — **a server-controlled flag makes a new cross-session message gate trust a field the SENDER supplies about itself**, converting a hold-for-human-review into an automatic ACCEPT for a receiver running with permission prompts bypassed. This contradicts the subsystem's own in-source contract, which states in as many words that the sender-supplied origin field is forgeable by any process running as the same user and must never be used to key identity. Same attribution-forgery class as #31. **ESCALATED at v2.1.248 (session-89): the flag's compiled DEFAULT flipped ON**, so the premise that made this a finding — “server-flippable on” — is now simply what ships. The mechanism is unchanged; what changed is reach, and **the channel inverts**: the server would now have to push the flag OFF to restore the human-approval hold for a receiver running with permission prompts bypassed. The binary's own schema concedes that the trusted field is only “as declared by” the sending host.
  - **#183 INFORMATIONAL + two LOW** (v2.1.221–v2.1.241 range) — **a new class of local execution.** A remote session can drive shell commands on the user's LOCAL machine over an outbound socket, with the output returning to the REMOTE session's model rather than to the local one. The important design fact is that this path **bypasses the local permission system entirely** — no per-call permission check, no permission mode, no allow/deny rules, no local pre- or post-tool hooks — substituting the operating-system sandbox instead, whose preconditions fail CLOSED at six checkpoints, two of them guards written specifically against sandbox-escape pivots. It is inert on a stock install behind an explicit command-line action, two default-off server flags, an organisation policy that fails closed, a signed device binding whose server echo the client verifies, and a sandbox opt-in that is off by default. Filed INFORMATIONAL because the substitution is coherent and the gating is genuinely layered — but it belongs in the harness map, because a reviewer who assumes that ALL local tool execution passes the local permission check would now be wrong.
  - **#184 MEDIUM** (v2.1.221–v2.1.241 range) — **a fast path that auto-approves writes into verified linked repository worktrees outside the declared working set, skipping the classifier.** The only CONFIRMED permission widening out of the entire default-off sweep.
  - **#185 (method gap, tracker)** — **a default is not what is served.** Every flag census this project had ever run read the DEFAULT out of the binary and assigned severity from it; for a server-controlled configuration channel that answers the wrong question, and the client has held the right answer all along in its own local cache of the values the server actually served. The first census-versus-cache diff showed **17 of the 55 newly-added default-off gates switched ON for this account**, four of them paths that carry externally-authored text into model context; the inverse check was clean (of 76 default-on gates, three are served off, none a permission decision). Now standing procedure. It **reversed a severity call mid-session**: a finding filed HIGH, downgraded to LOW on the reasoning that it needed a second default-off gate flipped as well, then RESTORED to HIGH once the cache showed both gates already on — the downgrade was right about defaults and wrong about reality.
  - **#193 HIGH (method)** (v2.1.242) — **anchor-table blindness.** The reason #195 was missed is the reusable part of that window: every agent chose its decode targets from the standing anchor table, which is by construction a list of the PREVIOUS window's literals — so the release's largest new subsystem was invisible to the entire method. There is still **no census of the model-context surface as a class**, which is the structural fix this finding asks for.
  - **#195 HIGH** (v2.1.242) — **a plugin-supplied hook-module runtime that SUBSTITUTES model-facing text rather than appending to it.** A registered handler can replace the tool description sent to the model, and prompt-section text, with no delimiter and no attribution; validation amounts to a type check plus a 32,000-character cap. It sits behind a **default-off internal environment gate that is absent from the served configuration cache**, so the server can arm it. Filed HIGH with the promotion gate being **provenance** — whether a server-influenced plugin can carry such a module — rather than shape. **That gate was answered in session-89 and NOT cleared**: registration is not restricted to locally-installed plugins, but no path lets the server supply the module CONTENT, so the server arms the surface and the plugin distribution channel supplies it.
  - **#196 / #197 / #198 / #199 HIGH** (v2.1.246–v2.1.258) — four further server-pushed-string paths into model context, in the #106/#154/#165/#168 family. **#199 is the sharpest shape in the whole lineage: serving its flag TRUE turns an automatic-mode consent rule OFF**, by excising a named rule from the safety classifier's own instruction text. A census that reads compiled defaults therefore scores a **consent-REMOVING** control as benign — the polarity is inverted relative to every other member of the family.
  - **#200 MEDIUM** (v2.1.246–v2.1.258) — **a third flag-reader shape is invisible to two independent census instruments.** A hoisted name paired with an inline default is matched by neither, because each looks for a quoted name ADJACENT to a default; the default-true population was therefore a **pre-existing UNDERCOUNT**, not a smaller true population. A fourth shape is now known. This is why the census figure was CORRECTED mid-window rather than merely bumped.
  - **#201 HIGH** (v2.1.251) — **the #195 substitution surface widened, and one arm of it LEAVES THE MACHINE.** One substitution kind rewrites the trailer block that lands in every commit message and pull-request body — the first reach in this family that escapes the local session — and another replaces whole skill bodies.
  - **#202 HIGH** (v2.1.251) — **a project-scope environment blocklist that omits the content-logging family.** Filed on v2.1.251's tracing fix, re-scoped out of High by adversarial review, then **RESTORED in session-90 once the deciding read was actually run**: the review was right that the first evidence was invalid, and wrong to expect a refutation. The project-scope filter consults its own blocklist, which omits that family, while the collection that DOES carry those names guards administrator settings tiers on a different path entirely. Scoped as a **stock-machine** finding — on a managed deployment a higher-trust telemetry claim reclaims the destination setting from lower-trust scopes; on an unmanaged one it does not.
  - **#203 HIGH** (v2.1.246–v2.1.258) — **the system prompt is recorded once and reused verbatim** on every later request and on resume, with a corrected relaunch ignored until compaction. It adds no new injection path; it **MULTIPLIES the lifetime** of every injection path this project already tracks, which is why it sits in this list rather than beside it.
  - **#204 MEDIUM** (v2.1.246–v2.1.258) — two new model-switch hooks that can cancel a switch and that carry a documented model-context field: a scope finding rather than a new vector.
  - **#206 HIGH** (v2.1.246–v2.1.258) — **of the messages in the cross-session approval family, the response that SETS THE RECEIVER'S PERMISSION MODE is the only one carrying no sender binding at all**, while every sibling pins the sender identity to the envelope. It overturned a “verified non-finding” recorded earlier in the same window, and **the evidence that refuted it was already in hand when the wrong conclusion was written** — so it is recorded here as a method result as well as a finding. Same attribution-forgery class as #31 and #182.
  - **SECURITY-POSITIVE, and tracked as such** (v2.1.251/v2.1.257) — v2.1.251 shipped five upstream fixes, all verified still present at v2.1.258, and v2.1.257 **deletes a bundled path-walking dependency outright**. The resulting fall in path-resolution call sites READS like a regression and is the opposite: the replacement primitive is a file-descriptor-and-handle check rather than a path re-resolution, and every defence axis measured GREW. Recorded because a count-only reading of this window would have scored a hardening as a loss.
  - **DEFENDED, and tracked as such** (v2.1.221–v2.1.241 range) — the same runner subsystem behind #176 also ships **the first named, logged sanitiser on the server-input channel anywhere in this codebase**: it strips privileged operator tool names out of the server-supplied argument map before spawning, with an explicit log line, and the child-environment builders null out the subsystem's own secrets so they are not inherited. It is carried as a **defence anchor** — an anchor whose **disappearance** is the alarm rather than its presence, inverting how every other anchor in the set reads.
- **#31 AC3** (subagent ghost-inbox / attribution-forgery class) is **NARROWED, NOT RETIRED, as of v2.1.241** — it was carried as UNDEFENDED through v2.1.220. The v2.1.221–v2.1.241 range gives it a real defence for the first time: a NEW inbound gate that fails CLOSED on every ambiguity, keys peer identity on kernel socket credentials rather than on anything in the message payload, and surfaces a *claimed* name separately from a *verified* process identity in a human approval dialog whose text sanitisation was attacked and HELD. The binary now concedes the finding's core claim in its own source. Four reasons it is not retired, each conceded by that same source: the verified identity **never reaches the model** (the model-visible wrappers carry no such field); the in-process send path calls the delivery primitives directly and is therefore never classified by the gate at all; the verified identity is absent on some platforms and identifies the CONNECTING PROCESS rather than the message author; and process identifiers are recyclable. **Any statement that #31 is simply undefended is now stale and must not be repeated unqualified.** PRIOR window record (still accurate as history): a v145 guard addresses skill self-recursion only, orthogonal to the inbox path. (The v217 subagent nesting-depth change behind #169 is *adjacent* rather than a defense: it shrinks the default nesting surface #31 rides on from a hardcoded 5 to 1, but changes no attribution check.) Anchor literals were byte-stable v145→v217 — the drifts there are all benign NEW legitimate producers stamping the field from a REAL source id (the v205 end-conversation abort branch; a further single new error-path producer in the v207–v212 window), each with the consumer that #31 exploits byte-identical, so the gap is neither worsened nor fixed. (The v160 sub-agent-output security-warning surface is an output content-check, orthogonal to inbox attribution — watch W5; a v176 forged-turn detector is telemetry-only and adds no new defense to the inbox path; the v199 SendMessage misroute fix is a RECIPIENT-side guard — it refuses a silent re-send to a reused name after the resolved member has left — orthogonal to the sender-side attribution forgery #31 exploits, so #31 AC3 is neither mitigated nor regressed.)
- **#140 HIGH** (v2.1.158, plugin-sync) — a new opt-in plugin-sync leg (an environment-variable gate, default off, plus org auth — not server-flippable) fetches an org's enabled-plugin list from an authenticated org-scoped endpoint and headlessly registers each synced plugin's MCP servers, **including subprocess-spawning (stdio) servers**, via a non-authoritative refresh with **no consent prompt in the sync caller**. This is asymmetric to the sibling skills-sync leg, which deliberately suppresses hook registration. The sync framework itself predates v158 (the skills-sync env-gate is present in v156); only the plugin leg is v158-new. Promotion-gate to CRITICAL = a runtime confirmation that the headless refresh bypasses the normal MCP first-use trust prompt for org-synced subprocess servers. Chains #136 + #110.
- **#136 HIGH** — a server-pushed plugin-allowlist returns the live OAuth bearer (`ANTHROPIC_AUTH_TOKEN`) for each on-list plugin, which the caller then merges into the env of the spawned plugin's `hooks/hooks.json` subprocess. The credential-return idiom is genuinely new code in v144. Marketplace-name spoofing (Critical promotion path) is closed by a reserved-name validator on the 9 known Anthropic marketplaces; HIGH stays. Filed session-60.
- **Skills-sync surface** (v152, registered, NOT a new disclosure): an org-scoped server-pushed skill content sync delivers per-skill zip archives that the client extracts into the local skills directory. Multistore-class defense-in-depth (org auth + path validator + zip-slip-defended extraction via a regex that blocks `..` traversal at start/middle/end + size/count limits + atomic rename). Promotion-gate to disclosure-candidate is a crafted-zip runtime probe to confirm whether a sync'd skill's `hooks/hooks.json` auto-registers `command`-type hooks.
- **Runtime probing.** Findings gated behind the interactive TUI require `tools/probe-sandbox/` + `tui-driver.exp`. `--print` and `claude doctor` short-circuit before the Ink React tree mounts, so downstream gates never fire.

## Workspace Transcript Access

When the Buddy Workspace is running (`cd workspace && pnpm dev`), the transcript is persisted to `workspace/.transcript/current.jsonl`. Use the `workspace-mcp` tools to read buddy reactions and workspace state:

- **`read_transcript`** — Read recent transcript entries from user, Claude, and buddies. Use when you want to see what buddies (Shingle, Ponder, Gust, etc.) have said. Supports `source` and `buddyName` filters.
- **`search_transcript`** — Search transcript entries by keyword.
- **`get_workspace_status`** — Get active session preset, buddy roster, skills, and cooldown timers.

## Dependencies

### agent-almanac (optional, recommended)

The workspace's PTY Claude benefits from agent-almanac skills, agents, and teams. Currently available via global symlinks (`~/.claude/skills/`, `~/.claude/agents/`) pointing to a local clone.

**Local development**: No action needed — global symlinks are sufficient.

**Deployment elsewhere**: Clone agent-almanac and run its discovery installer, or add project-local symlinks:
```bash
# Option A: global install (recommended)
git clone https://github.com/pjt222/agent-almanac.git /path/to/agent-almanac
cd /path/to/agent-almanac && claude /install-almanac-content

# Option B: project-local (fallback)
mkdir -p .claude/agents .claude/skills
ln -s /path/to/agent-almanac/agents/*.md .claude/agents/
ln -s /path/to/agent-almanac/skills/* .claude/skills/
```
