# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Purpose

Research repository documenting findings about Claude Code's built-in "Buddy" companion system, the "Advisor Strategy" system, and — since v2.1.118 — the wider Claude Code harness (hooks, background daemon, MCP channels, server-controlled config channels). The companion UI was removed in v2.1.97, but the server-side reaction API is still alive. The advisor system (tool name `advisor_20260301`) was code-complete in v2.1.96, coexisting with the full buddy, and is dark-launched behind an advisor feature gate. Our workspace and MCP tools call the buddy API directly — multi-buddy reactions work on any version. From v2.1.118 onward the investigation has broadened into a rolling per-version security audit of the harness; the current focus is the server-to-client config/control channel and its wire-confirmed injection primitives.

## Repository Structure

- `digest.md` — comprehensive investigation findings from 21+ agents across 4 waves
- `architecture.md` — companion technical deep-dive: function reference, data flow diagrams, API protocol, security boundary
- `advisor-architecture.md` — advisor system spec: tool lifecycle, system prompt, feature flags, telemetry, buddy comparison
- `config-excerpt.json` — companion config extracted from `~/.claude/backups/` state files
- `links.md` — 20 reference sources organized by category (official docs, source analysis, reverse engineering, prior art, competitors)
- `SECURITY-AUDIT.md` — original 14-finding tooling audit (1 CRITICAL, 3 HIGH, 5 MEDIUM, 3 LOW, 1 OBSERVATION). The live security tally has since grown well beyond this: `docs/counts.js` tracks an audit-baseline lineage (30 items: 7 critical / 9 high / 10 medium / 3 low / 1 observation) and a live re-derivation of 14 critical / 38 high / 59 medium / 13 low across 164 repo issues (session-79, 2026-07-17; the +1 high is #165, and the medium/low/issue-count growth folds in a catch-up of an earlier issue batch, re-derived directly from the tracker). `docs/counts.js` is authoritative — do not contradict it.
- `README.md` — project overview and per-version status timeline
- `tools/buddy-config.mjs` — CLI to read/modify companion config (Node.js 18+, zero deps)
- `tools/version-check.mjs` — pre-flight version compatibility check against installed binary
- `tools/bubble-tracking.md` — complete bubble tracking guide (lifecycle, capture strategies, timing constants)
- `tools/capture-timing.mjs` — post-session timing analysis (latency, cooldown gaps, TTL estimation)
- `tools/shared/bones.mjs` — deterministic companion trait derivation (FNV-1a + Mulberry32 PRNG)
- `tools/shared/config.mjs` — unified Shingle config reader (`~/.claude/shingle.json`)
- `tools/shingle-capture/` — dual-strategy capture (terminal scrape + API replay) with Claude Code hooks
- `tools/shingle-mcp/` — MCP server for programmatic buddy reaction access
- `tools/mempalace-sync.mjs` — sync captured reactions into MemPalace (cross-session memory)
- `tools/mempalace-setup.md` — integration guide: palace structure, MCP registration, automation
- `tools/sessions/` — multi-buddy session presets (deep-focus, debug-squad, dream-lab, full-crew)
- `tools/probe-sandbox/` — containerized MITM probe harness (two-service docker compose: a proxy + claude-code on an isolated bridge network; filesystem/process-isolated from the host's live REPL). Used for runtime wire-confirmation of server-controlled-channel findings.
- `tools/probe-sandbox/probes/lib/tui-driver.exp` — `expect`-based PTY keystroke automation that drives a real interactive Claude Code TUI past login + theme picker. Required for any finding gated behind the Ink React tree (`claude --print` short-circuits before it mounts).
- `tools/mitm-harness/` — host-side MITM (simpler, no filesystem isolation; use when a probe must mutate `~/.claude/*` or run alongside live host sessions)
- `workspace/tools/workspace-mcp/` — MCP server for reading workspace transcript and status
- `tools/test-protocol.md` — empirical test protocols for bubble TTL and narrow terminal behavior
- `results/` — per-session probe writeups (round-1 flag deltas, runtime probes, gap analyses) — the long-form source of truth behind every claim summarized in `README.md`
- `docs/` — GitHub Pages visualization (Three.js, Viridis dark theme); `docs/counts.js` is the single source of truth for every displayed count

## Key Context

- The buddy system is a first-party Claude Code feature (v2.1.89+, Pro/Max plan), launched April 1, 2026
- Companion identity is deterministic: Bun.hash (wyhash) of user ID with a fixed salt string, feeding a Mulberry32 PRNG (FNV-1a is the Node.js dev fallback only)
- Only 3 fields persisted in `~/.claude/.claude.json`: name, personality, hatchedAt. All other traits re-derived from hash each session.
- Shingle is architecturally separate from the main Claude Code agent — strictly unidirectional (observes but cannot write back)
- 6 reaction triggers: turn, hatch, pet, test-fail, error, large-diff (complete, idle, silence were debunked)
- Binary at `~/.local/share/claude/versions/`. Version chain probed: v2.1.90/v2.1.92 (companion), v2.1.96–v2.1.100 (advisor), v2.1.101/v2.1.104 (loop system), v2.1.105–v2.1.114 (harness/gate-surface), v2.1.116–v2.1.160 rolling per-version flag-diff (skips: v2.1.120/122/124/125/127/130/134/135/136/137/139/146/149/150/151/154/155/157), **v2.1.161–v2.1.177 retro-audited in one pass (session-67; two versions unpublished on npm)**, **v2.1.178–v2.1.191 audited (session-69; four versions unpublished)**, **v2.1.193/v2.1.195 (session-71; two versions unpublished)**, **v2.1.196 audited (session-72)**, **v2.1.197 audited (session-73; ZERO new findings)**, **v2.1.198–v2.1.200 audited (session-74; ZERO new findings)**, **v2.1.201–v2.1.206 audited (session-77/78; ZERO new findings, ZERO remediations, ZERO regressions)**, **v2.1.207–v2.1.212 audited (session-79; ONE new finding — #165 HIGH — ZERO regressions, multiple hardening wins)**. **Current binary = v2.1.212** (npm `latest`=v2.1.212); stable release = **v2.1.197**. Each release is a genuine per-release build: the app build id changes every release. (An earlier framing that read v160→v177 as rebuilds of one source tag — with a single change around v178–v181 — was a tracking error; it followed a runtime-embedded constant that only rotates on a bundled-runtime bump, not the app build id.) The bundled runtime did take one major bump around v178–v181 and has been stable since, but v181→v200 are genuine per-release builds, not rebuilds of a frozen tag (v196 = +~1 MiB, v197 = +~143 KiB, v198–v200 = +~5.78 MiB total, all reconciling as pure compiled-bundle code — no asset/native/blob markers). v158 adds an opt-in plugin-sync leg (an environment-variable gate, default off) atop the pre-existing skills-sync framework — **#140 HIGH**: it headlessly registers an org's synced plugins' MCP servers (including subprocess-spawning ones) with no consent prompt in the sync caller, asymmetric to the skills-sync sibling which suppresses hook registration. **v159 trivial; v160 = major feature release** (a flag-name-only diff structurally HID 39 new client env-var literals + a **Cowork remote-environment bridge** [runner-side only] + a **two-stage auto-mode safety classifier** [fail-CLOSED = strengthening, opposite of the #108 inversion]). v161-177 findings: **#151 HIGH** (v174 — a server-flippable flag re-enables the git credential helper on the background plugin-marketplace auto-update fetch; locally reachable, not cloud-runner-gated; #136/#140 family) and **#152 LOW** (v166 — a server-flippable flag skips the Edit/Write read-before-write guard; data-integrity only, no permission reach). Two major hidden-code releases (a refusal-fallback state machine; an artifact/frame-deploy publish subsystem) decoded benign/below-bar. **v178-191 findings (session-69): #154 CRITICAL** (a v179 server-pushed string flag — empty default, no length cap — wired verbatim into the system-prompt dynamic-section builder, reaching the `/v1/messages` **system** field at `role:system` verbatim; **wire-confirmed** on an interactive TUI, an injected marker reached the system field; #106-class but a distinct flag with a `role:system` sink) and **#155 HIGH** (a v187 server-pushed, schema-validated styled-string array rendered in the startup banner via the Ink text component; a byte-test showed Ink/chalk STRIPS the dangerous escape classes — cursor/clipboard/DSR/OSC8 hyperlink — only color surviving → the Critical/phishing vector REFUTED; residual = server-controlled styled-text spoofing in the trusted startup banner; #127-sibling). **#108 CRITICAL REMEDIATED upstream at v179** (the default-true sandbox-classifier fail-open inversion was removed; 0 occurrences v179+; retained in the finding census as a remediated Critical). **session-70: #127 DEMOTED Critical→High** (a byte-level re-test on the v191 image proved the notification string also renders via the Ink text component, which strips the dangerous escape classes; the session-59 "unsanitized ANSI + bare phishing URL" Critical was a substring grep of one benign color class — the dangerous classes were never tested and are in fact stripped; residual = styled-text spoofing in the trusted notification banner; GitHub relabeled critical→high-priority). **v193-196: ZERO new findings, ZERO regressions.** v196 ships one hardening default-flip — an anti-MITM guard on artifact upload flipped default-OFF→ON (now on by default = HARDENING, the inverse of a fail-open) — plus one benign headline feature: a plugin binary-asset provisioning path that fetches sha256-pinned binaries into a plugin's `bin/` from a first-party content-addressed store, gated to OFFICIAL marketplaces only, default-off, with the harness NOT executing the placed files (execution stays mediated by the plugin's own already-trusted hooks); it is the best-hardened member of the plugin-credential family (#136/#140/#151) — a WATCH, not a finding. **v197: ZERO new findings** — a small model-catalog-preparation feature build; two new benign surfaces: (1) a new server-pushed config-cache key holding a promo-expiry date, hard-sanitized (re-parsed through a date formatter that discards the raw string) and shown ONLY in the model-picker UI — it never reaches model/system context or a permission gate; (2) a new documentation-only API endpoint (reference text in a bundled skill, no in-harness caller). **v198–200 (session-74): ZERO new findings, ZERO remediations, ZERO regressions.** v198 is a MAJOR feature release (browser automation went GA; subagents now run in the background by default and, when launched from the background-agents view, auto-commit/push and open a DRAFT pull request on finishing code work; a new gateway upstream provider; a host-managed-credentials file path; a chart-design skill; a design-consent API); v199 adds fixes + request-body compression + stacked slash-commands + a new teleport repo-host verification guard + a SendMessage misroute fix; v200 (npm `next`) adds an "observer agents" capability + background-agent auth-mismatch guards. Every surface decoded benign: (a) the host-managed-credentials file reader is the best-hardened member of the credential family (#136) — owner-only-permission + schema + process-liveness + expiry checks at the use-site, tokens held in memory and kept OUT of the inheritable subprocess environment, double-gated behind two operator-set environment variables with no server-push source, not reachable from a plain local session; (b) parked-permission resume for background/away-from-keyboard agents is FAIL-CLOSED — a deferred permission auto-resolves only by replaying the user's own persisted answer; a timeout cancels and re-asks, never auto-approves; (c) the background auto-push/draft-PR is worktree-isolated and DRAFT-only, and the merge/force-capable auto-approve allowlist is scoped to a user-invoked commit-push-PR command, absent from the background path; (d) the away-from-keyboard question auto-advance submits ONLY answers the user already selected (unanswered questions become "skipped", never auto-picked) and is barred from plan/permission approval; (e) the new teleport repo-host telemetry sits inside a NEW repo-binding guard that refuses on a repo mismatch — added defense, not a masked hole; the #99 teleport-chain layer is unchanged; (f) the design-consent endpoint sends only a boolean, and artifact upload stays user-invoked behind design-OAuth login + recorded consent + the default-on anti-MITM upload guard; (g) the new "observer agents" capability (a background agent that watches another) has a default-true gate that is INERT behind an operator experimental environment variable (no server-push can enable it alone), and an observer influences the observed agent only through a harness-mediated report channel (the inbox/SendMessage path is blocked). **Two new WATCH items** (functional, not findings): W-BGPUSH — background auto-push/draft-PR is gated, but the never-push-to-main / no-force / no-merge boundary is prompt-level, not code-enforced; W-OBSERVER — the observer-agents default-true gate is inert behind an operator experimental env, re-check if a future release drops that env gate. Browser-automation GA is OUT OF SCOPE for the server-channel audit: the tooling pre-dates v198 (which only flipped it to GA), and the diff adds no server-controlled browser gate. **v201–v206 (session-77/78): ZERO new findings, ZERO remediations, ZERO regressions.** v201 is a near-pure refactor — a genuine per-release build with net-zero size growth and no new surface once greedy-grep artifacts are discounted. v202 ships a diagram-rendering feature for artifacts (+~10 MiB = a diagram-render + diagram-parser + HTML-sanitizer stack that itself SHIPS AN XSS SANITIZER for the rendered diagram output = a security-positive), plus two benign surfaces: a cloud-runner agent-proxy that MEDIATES credentials (it injects a sentinel token into the tool subprocess environment and keeps the REAL credential OUT of it — the inverse of a credential leak) and a refusal-fallback auto-retry (a server-pushed boolean that swaps to a fallback model on an availability refusal — an availability swap, NOT a text-into-model-context primitive). v203 carries a ~5 MiB code SHRINK — a preview/render engine RETIRED after the diagram path landed (pure code removal, no embedded blob; a shrink can neither add nor mask a finding, and all standing anchors stay byte-stable); its four safety-relevant candidates all decoded benign — (a) an auto-mode edit-classification capability, default-OFF, that when on routes edits to MORE scrutiny (the INVERSE of the removed #108 fail-open, not a safety inversion), (b) a NEW default-true daemon-side downgrade-refusal guard (security-positive: the background daemon refuses to self-restart into an OLDER on-disk build; server-disableable, but disabling only reverts to prior behavior — #113-adjacent, no new reach), (c) a new provider-auth environment variable for an already-scaffolded provider backend (operator-set, held in the credential-redaction lists), and (d) a resume-integrity filter that DROPS unlinked transcript records on resume (the opposite of an attribution-injection). v204 is a mechanical rebuild (a tiny code re-chunk; zero flag/env/gate changes). v205's headline is a security-POSITIVE auto-mode exfil-command awareness enrichment: the auto-mode safety classifier now flags exfil-capable git/gh commands (push, remote set-url/add, PR/issue create, release upload, fork) and enriches its permission decision with the repo's public/private visibility (default-off, client-computed, sanitized) and git-status PATHS (default-off, paths/status only — NEVER file content, length-capped, sent to the FIRST-PARTY classifier, no third-party sink); it is a risk HINT, not a decision (no visibility branch flips a permission), net-defensive and analogous to the v160 two-stage classifier. v206 is a feature build (+~1.49 MiB compiled code) — staged-tool-call gating (a server-controlled KILL-SWITCH: turning it OFF REFUSES the staged call = a capability reduction, not a fail-open), a new end-conversation agent-lifecycle tool (guarded — a subagent cannot end the parent conversation; local-only, no egress), a plan-review UI, and a telemetry cluster. **Two standing-finding anchors drifted by ±1 at v205 and BOTH decode benign:** the #110 field-egress anchor fell by one occurrence (a semantics-preserving hoist of a telemetry emit above a branch — the egress is unchanged, NOT remediated), and the #31 AC3 attribution anchor rose by one (a NEW legitimate producer — an end-conversation abort branch stamping the field from a REAL source id — with the consumer that #31 exploits byte-identical, so the gap is neither worsened nor fixed). One enable-gate GRADUATION: a design-integration enable environment variable was removed while the consent+guard boundary on its egress stayed byte-stable (feature-GA, not a masked hole). **Two new WATCH items** (functional, not findings): the daemon downgrade-refusal guard is server-disableable (net-positive, #113-adjacent); the v205 auto-mode exfil-awareness enrichment (net-defensive). Standing findings #106/#110/#154/#151/#127/#155 reproduce byte-identical v197→v206; #108 stays removed (0). **v2.1.207–v2.1.212 (session-79): ONE new finding — #165 HIGH — ZERO regressions, multiple hardening wins.** Six genuine per-release builds (distinct app build id each), runtime unchanged; the full +~5.27 MiB is compiled application code confined to the bundle section (readelf-localized — the native-code and read-only-data sections are byte-identical across the installed releases, and no embedded blob appears in any of the six string-pair diffs). **#165 HIGH** = a NEW server-push-into-model-context primitive: a server-controlled configuration value can override an OFFICIAL-marketplace or built-in plugin's model-facing text — the MCP server-instructions plus the tool/parameter/prompt/skill descriptions handed to the model — and that override text lands in model context VERBATIM. The payload is type-validated only (no content sanitization); the primary server-instructions field is length-capped but not escaped, the other maps uncapped. It is kin to the stop-hook #106 and system-prompt #154 instances but a distinct flag/key/sink-role, with the injection attributed to a trusted first-party plugin's instructions. Filed HIGH not Critical because it is default-off, gated to official/built-in plugins only (third-party plugins cannot be targeted), fails safe to the built-in text on a malformed payload, and is not yet wire-confirmed — wire-confirmation is the natural escalation. **Hardening this window:** a transient v207 feature that briefly routed a server-pushed prompt string into model context (gated behind a remote/cowork entrypoint, never reachable on a plain local session) was REMOVED one release later; a new default-off toggle ADDS authentication to the server-controlled config channel; the memory subsystem's secret-skip guard survived and hardened a refactor (it now hard-blocks secret-bearing memory writes, fail-closed); the read-before-write guard-skip on the file-edit tool became NON-server-flippable (the server can no longer force-skip it — the write-path residual stays Low, #152); a NEW guard code-enforces the never-reuse-the-default-branch rule for the cloud/teleport auto-PR path (partially closing the prior background-push watch item W-BGPUSH); and the anti-downgrade guard on the background daemon is now unconditional (no server flag on its predicate). Standing findings #106/#110/#154/#151/#127/#155 reproduce byte-stable v206→v212; #108 stays removed (0); the #31 subagent-attribution anchor moved by a single benign NEW error-path producer whose consumer is byte-identical. **Tooling fix**: a per-version delta-extractor blind spot (a minified accessor identifier that legitimately contains a `$`, which the extractor's character class excluded) was found and fixed — it had briefly made a stable server-push config set read as removed. Methodology rule reinforced: the non-flag diff (client env vars / server-pushed client-cache keys / API endpoints / runtime version) is mandatory per release, and the credential-env diff must be run prefix-agnostically (an env-name diff scoped to a few known prefixes is blind to provider/secret prefixes outside them); reconcile binary-size delta vs strings-byte delta. DEFAULT-TRUE **22→28→29→30→32→33→34→35→43** v160→v177→v191→v196→v200→v202→v203→v206→v212 (v212 = 41 boolean + 2 typed; the typed default-true count dropped 3→2 at v179 when the #108 gate was removed and has stayed 2 since; the boolean additions across the window are all benign — v196 an anti-MITM upload guard flipped default-ON, v198–200 an explore/plan helper-agent toggle and an observer-agent gate, v202 a diagram-render capability toggle, v203 the daemon downgrade-refusal guard, v206 the staged-tool-call kill-switch, and v207–v212 eight further additions each of which either reduces capability on a server OFF-flip or is a reliability/UI/approval-fail-closed toggle — and NONE is a safety-gate inversion).
- Date gate bug: a date-based gate covering Jan–Mar each year disables the companion during those months on any year from 2026 onward

## Advisor System Context

- The advisor (tool name `advisor_20260301`) is a server-side Messages API tool — NOT a separate endpoint like the buddy reaction API
- Valid advisor models: `["opus", "sonnet"]` — `"sonnet"` resolves to `claude-sonnet-4-6`; `"opus"` resolves to `claude-opus-4-8` for tier-eligible accounts (new v2.1.156), `claude-opus-4-7` non-eligible fallback, `claude-opus-4-6` deeper fallback. See `advisor-architecture.md` §3.
- Feature gate: composed of a feature-disable env-var check, a first-party auth check, and an advisor feature flag
- CLI flag: `--advisor <model>` (hidden until flag rolls out)
- Slash command: `/advisor [opus|sonnet|off]` (hidden until flag rolls out)
- Key binary functions (paraphrased by role): a feature-gate predicate, two input validators, two cost-tracking helpers, and two system-prompt assembly variables
- 5 telemetry events: advisor command, dialog shown, tool call, tool interrupted, tool token usage
- Coexisted with buddy in v2.1.96 — was NOT built as a replacement

## Kairos Loop System Context

- The Kairos loop (v2.1.101+) provides autonomous self-continuation via the `ScheduleWakeup` tool and `/loop` slash command
- `ScheduleWakeup(delaySeconds, prompt, reason)` — runtime clamps delay to `[60, 3600]` seconds; dynamic loops use `CronCreate(kind: "loop")` internally
- Four prompt sentinels: `<<autonomous-loop>>`, `<<autonomous-loop-dynamic>>`, `<<loop.md>>`, `<<loop.md-dynamic>>`
- Feature gates: a loop scheduler gate (tool), a slash-command gate, and a sentinel-resolution gate
- Loop ages out at 7 days (`recurringMaxAgeMs: 604800000`, configurable up to 30 days)
- Key binary functions (v2.1.101, paraphrased by role): a wakeup scheduler, a default-sentinel resolver, and a file-sentinel resolver
- 2 telemetry events: loop-dynamic wakeup scheduled, loop-dynamic wakeup aged out; `/loop` invocation is also telemetered
- Full technical spec: `loop-architecture.md`

## Server-Controlled Channels Context (post-v2.1.118)

From v2.1.118 onward the investigation's primary focus is the **server-to-client config/control channel** — a path that pushes feature flags and string-typed config to the client at startup. It is undocumented in official Claude Code docs (see `results/docs-gap-analysis-2026-05-20.md`).

- **Flag readers rotate per binary release.** Cosmetic minifier churn — never anchor cross-version checks on reader identifier names. The boolean reader and the typed reader each rotate identifiers across recent releases. Use string-pool literals (flag names, telemetry events, error messages) for cross-version claims, not minified identifiers.
- **7-layer flag resolution**, ~410 gate reads, **43 DEFAULT-TRUE at v2.1.212** (41 boolean + 2 typed); the typed default-true count dropped 3→2 at v179 when the #108 sandbox-classifier fail-open gate was removed upstream and has stayed 2 since; the boolean count grew via benign additions only — v196 an anti-MITM upload guard flipped default-ON, v198–200 an explore/plan helper-agent toggle and an observer-agent gate, v202 a diagram-render capability toggle, v203 a daemon-side downgrade-refusal guard, v206 a staged-tool-call kill-switch, and v207–v212 eight further additions each either reducing capability on a server OFF-flip or a reliability/UI/approval-fail-closed toggle — NONE a safety-gate inversion. (Historical waypoints: 20 DEFAULT-TRUE at v2.1.152 — 17 boolean + 3 typed, after v145→v152 added a workflows-master gate v147 and a daemon-binary-takeover gate v152; byte-identical v143→v145 baseline.)
- **Wire-confirmed server-push injection primitives** (all ride the same server-controlled channel; details in `results/`), referred to by finding number:
  - **#106 CRITICAL** — an empty-default string-flag overrides the hardcoded Stop-hook reminder; reaches the model context as a synthetic user-text message verbatim, no length cap, no cert pinning.
  - **#108 CRITICAL (REMEDIATED upstream at v179)** — a default-TRUE flag, server-flippable safety inversion of the sandbox/auto-mode permission classifier (fail-open). The gate was removed at v179 (0 occurrences v179+); retained in the finding census as a remediated Critical.
  - **#110 CRITICAL** — raw plugin/skill/marketplace identifier field-level egress on the event-logging batches (hundreds of raw fields per session, wire-identical v129→v145; anchor byte-stable through v212 — the one v205 drift, one fewer occurrence, is a semantics-preserving hoist of a telemetry emit above a branch, egress unchanged, NOT remediated).
  - **#113 HIGH** — a server-pushed forced-downgrade primitive. Wire-confirmed on an interactive TUI (session-59): the auto-updater performed the downgrade with no user prompt.
  - **#154 CRITICAL** (v179) — a NEW server-pushed string flag (empty default, no length cap) wired verbatim into the system-prompt dynamic-section builder, reaching the `/v1/messages` **system** field at `role:system` verbatim. **Wire-confirmed on an interactive TUI** (session-69): an injected marker reached the system field verbatim. #106-class but a distinct flag with a `role:system` sink.
  - **#127 HIGH** (was Critical, demoted session-70) — a server-pushed terminal-notification string rendered in the trusted TUI notification banner. A byte-level re-test (session-70, v191 image) proved it renders via the Ink text component, which **strips** the dangerous escape classes (cursor/clipboard/DSR/OSC8 hyperlink) — only color survives. The earlier session-59 "unsanitized ANSI + bare phishing URL" Critical was a substring grep of one benign color class; the dangerous classes were never tested and are in fact stripped. Residual = server-controlled styled-text spoofing in the trusted notification banner (no cloaked link / clipboard / model-context / credential reach). GitHub relabeled critical→high-priority.
  - **#155 HIGH** (v187) — a NEW server-pushed, schema-validated styled-string array rendered in the startup banner via the Ink text component (#127-sibling). A byte-test showed Ink/chalk strips the dangerous escape classes — only color survives → the Critical/phishing vector REFUTED; residual = server-controlled styled-text spoofing in the trusted startup banner.
  - **#165 HIGH** (v207–v212 window) — a NEW server-push-into-model-context primitive: a server-controlled configuration value can override an OFFICIAL-marketplace or built-in plugin's model-facing text (the MCP server-instructions plus the tool/parameter/prompt/skill descriptions handed to the model), and that override text lands in model context VERBATIM. Type-validated only (no content sanitization); the primary server-instructions field is length-capped but not escaped, the other maps uncapped. #106/#154-class but a distinct flag/key/sink-role, with the injection attributed to a trusted first-party plugin's instructions. HIGH not Critical: default-off, official/built-in plugins only (third-party plugins cannot be targeted), fails safe to the built-in text on a malformed payload, and not yet wire-confirmed (wire-confirmation is the natural escalation).
  - **#105 HIGH** — a server-flippable third-party-logging gate ships envelope identifiers plus a system fingerprint to a third-party processor. Extends an earlier envelope-leak finding.
- **#31 AC3** (subagent ghost-inbox / attribution-forgery class) **remains UNDEFENDED as of v2.1.212.** A v145 guard addresses skill self-recursion only, orthogonal to the inbox path. Anchor literals byte-stable v145→v212 — the drifts are all benign NEW legitimate producers stamping the field from a REAL source id (the v205 end-conversation abort branch; a further single new error-path producer in the v207–v212 window), each with the consumer that #31 exploits byte-identical, so the gap is neither worsened nor fixed. (The v160 sub-agent-output security-warning surface is an output content-check, orthogonal to inbox attribution — watch W5; a v176 forged-turn detector is telemetry-only and adds no new defense to the inbox path; the v199 SendMessage misroute fix is a RECIPIENT-side guard — it refuses a silent re-send to a reused name after the resolved member has left — orthogonal to the sender-side attribution forgery #31 exploits, so #31 AC3 is neither mitigated nor regressed.)
- **#140 HIGH** (v2.1.158, plugin-sync) — a new opt-in plugin-sync leg (an environment-variable gate, default off, plus org auth — not server-flippable) fetches an org's enabled-plugin list from an authenticated org-scoped endpoint and headlessly registers each synced plugin's MCP servers, **including subprocess-spawning (stdio) servers**, via a non-authoritative refresh with **no consent prompt in the sync caller**. This is asymmetric to the sibling skills-sync leg, which deliberately suppresses hook registration. The sync framework itself predates v158 (the skills-sync env-gate is present in v156); only the plugin leg is v158-new. Promotion-gate to CRITICAL = a runtime confirmation that the headless refresh bypasses the normal MCP first-use trust prompt for org-synced subprocess servers. Chains #136 + #110.
- **#136 HIGH** — a server-pushed plugin-allowlist returns the live OAuth bearer (`ANTHROPIC_AUTH_TOKEN`) for each on-list plugin, which the caller then merges into the env of the spawned plugin's `hooks/hooks.json` subprocess. The credential-return idiom is genuinely new code in v144. Marketplace-name spoofing (Critical promotion path) is closed by a reserved-name validator on the 9 known Anthropic marketplaces; HIGH stays. Filed session-60.
- **Skills-sync surface** (v152, registered, NOT a new disclosure): an org-scoped server-pushed skill content sync delivers per-skill zip archives that the client extracts into the local skills directory. Multistore-class defense-in-depth (org auth + path validator + zip-slip-defended extraction via a regex that blocks `..` traversal at start/middle/end + size/count limits + atomic rename). Promotion-gate to disclosure-candidate is a crafted-zip runtime probe to confirm whether a sync'd skill's `hooks/hooks.json` auto-registers `command`-type hooks.
- **Runtime probing.** Findings gated behind the interactive TUI require `tools/probe-sandbox/` + `tui-driver.exp`. `--print` and `claude doctor` short-circuit before the Ink React tree mounts, so downstream gates never fire.

## Workspace Transcript Access

When the Buddy Workspace is running (`cd workspace && pnpm dev`), the transcript is persisted to `workspace/.transcript/current.jsonl`. Use the `workspace-mcp` tools to read buddy reactions and workspace state:

- **`read_transcript`** — Read recent transcript entries from user, Claude, and buddies. Use when you want to see what buddies (Shingle, Ponder, Gust, etc.) have said. Supports `source` and `buddyName` filters.
- **`search_transcript`** — Search transcript entries by keyword.
- **`get_workspace_status`** — Get active session preset, buddy roster, skills, and cooldown timers.

## Dependencies

### agent-almanac (optional, recommended)

The workspace's PTY Claude benefits from agent-almanac skills, agents, and teams. Currently available via global symlinks (`~/.claude/skills/`, `~/.claude/agents/`) pointing to a local clone.

**Local development**: No action needed — global symlinks are sufficient.

**Deployment elsewhere**: Clone agent-almanac and run its discovery installer, or add project-local symlinks:
```bash
# Option A: global install (recommended)
git clone https://github.com/pjt222/agent-almanac.git /path/to/agent-almanac
cd /path/to/agent-almanac && claude /install-almanac-content

# Option B: project-local (fallback)
mkdir -p .claude/agents .claude/skills
ln -s /path/to/agent-almanac/agents/*.md .claude/agents/
ln -s /path/to/agent-almanac/skills/* .claude/skills/
```
